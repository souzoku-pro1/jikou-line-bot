# 2026-08-15 充足実測監査（基準=①210時間版 Phase 0〜9）

- TASK_ID: AUG-PLAN-AUDIT
- 実施: PC-A（READ_ONLY 実測＋本文書の収載のみ。コード・既存 docs は不触）
- 基準 SHA: `9406ca6`（branch `plan-audit`・起点 origin/main `f1b9dad`）
- 作用仕様: `docs/plan/` 収載の3文書（①②③）全文

## 0. 監査の前提

### 0.1 判定基準（司令塔裁定・2026-08-15）

判定基準は **①②（2026-07-10）** とする。③の工程・日割り・完了条件は SUPERSEDED として扱う。根拠（いずれも収載文書の原文）:

- ② L182: 「# 6. 旧96時間の日割り【SUPERSEDED・実行禁止】」
- ② L184: 「Claudeは日割り・順番・完了条件として使わず、長期設計正本第17章のPhase 0〜9を使う」
- ② L31: 「旧96時間日割り・P0〜P12・旧完了条件は履歴であり実行禁止」
- ① L116: 「本書第17章は210時間統合品質版の工程・工数・Gateの正本であり、96時間版・160時間版より優先する」
- ③ L335（③自身の記載）: 「本レポートと食い違う場合はより新しい裁定とリポジトリ実物が優先する」

したがって③は**成果物インベントリの索引**（第3節）としてのみ用い、工程・Gate・完了条件の判定には用いない。

### 0.2 判定語彙

| 語 | 定義 |
|---|---|
| DONE | ①の当該記述の要求が repo 実物で充足。根拠（パス・関数・PR番号）を示す |
| PARTIAL | 一部が実物で充足。未充足部分を明記 |
| NOT_STARTED | repo 実物に痕跡なし |
| 判定不能 | ①の要求が repo 外（[人]受領・production 設定・外部契約・Codex/大野の記録）にあり、repo からは真偽を確定できない。理由を必ず併記 |
| Gate: 通過／未通過／未検証 | Gate 条件が明示されている Phase について個別に付す |

**推測で埋めない**。特に production の env 投入状況・外部サービス契約・[人]の受領物・Codex レビュー実施記録は repo に無い限り「判定不能」とする。

### 0.3 参照した主な実測ソース

- `git log origin/main`（PR 番号・merge 日）／`docs/work-logs/**`／`docs/reports/2026-07_phase1-close-report.md`／`docs/automation-task-ledger.md`（2026-08-11 まで反映）
- code: `main.py`・`hub/**`・`channels/**`・`units/**`・`webapp/**`・`test_*.py`
- 全 suite 実測（本監査で実行）: 後述 §1.0

### 0.4 【重要】repo の P 系列番号と①の Phase 番号は一致しない

repo は 2026-07-11 以降 `P0`〜`P5` の系列名で作業しているが、①の Phase 番号との対応は**部分的にずれる**。判定を読む際の前提として先に固定する。

| repo 系列 | 実体（根拠） | ①のどこに当たるか |
|---|---|---|
| P0 | Phase 0 クローズ（`docs/work-logs/2026-07-11_Phase0クローズ_Phase1着手.md`） | ① Phase 0 |
| P1 | redaction 契約・durable 基盤・HMAC 署名移行・dependency lock（`docs/reports/2026-07_phase1-close-report.md`・PR #101–#141） | ① Phase 1 の一部 |
| **P2** | **残 lane 切替（koseki/registry/bank/valuation の署名化）・GAS drift・healthcheck 依存**（PR #143–#163・`2026-07-20_P2-koseki-cutover.md`） | **① Phase 1 の G1 条件（L3095「productionの全GAS／watcher callerが新認証へ移行」）の続き**。① Phase 2（UX・prototype）ではない |
| P3 | 導出台帳・封筒・関所・projection・取消（PR #165・#164・#168・#186・#187・#191・#193・#207） | ① Phase 1 成果物（App 36 DerivationRun／App 37／template registry）＋① Phase 3 の一部（journal・projection rebuild） |
| P4 | PWA 認証 shell・案件一覧/詳細・承認キュー参照・関係図（PR #173・#176・#182・#194） | ① Phase 3（control plane）＋① Phase 4（PWA 中核）にまたがる |
| P5 | 条項ライブラリ（PR #172） | ① Phase 5 |

**この不一致のため、repo が「Phase 1 完全クローズ」と記録していても、①の G1 が通過したことは意味しない**（§1.2 参照）。

---

## 1. ①第17章 Phase 0〜9 の充足実測

### 1.0 全体サマリ

| Phase | ①の工数 | 判定 | Gate | 一言根拠 |
|---|---|---|---|---|
| Phase 0 現況固定・containment | 10h | **PARTIAL** | G0 **未検証** | 証拠文書4種は実在（`docs/evidence/**`・`docs/current-state-report.md`）。G0 必須の P0_P16_GATE_TABLE・DESIGN_DOC_INDEX・GAS_WATCHER_RAILWAY_FINGERPRINT・LEGACY_MIGRATION_LEDGER・210時間再見積り・論理名mapping が repo に不在 |
| Phase 1 統合設計・hardening | 20h（G0後30〜40h） | **PARTIAL** | G1 **未通過** | RV-04/05/07/09/10/11/12/13 は実装・実機移行済み。**RV-02/03（/scan・/ocr/fixed-asset の無効化）が未達**（現行=無認証受理・`hub/service_auth.py:578`） |
| Phase 2 UX・prototype | 18h | **NOT_STARTED** | G2 **未通過** | INFORMATION_ARCHITECTURE・SCREEN_STATE_SPEC・Figma 6 journey・UX_TEST_RECORD 20件のいずれも repo に不在（①本文以外にヒット0） |
| Phase 3 PWA control plane | 34h | **PARTIAL** | G3 **未通過** | durable DB・InboundEvent・scheduler・webapp shell は実在。**passkey/WebAuthn・step-up・credential recovery が未実装**（認証=password+session cookie） |
| Phase 4 PWA 中核・adapter 統合 | 30h | **PARTIAL** | G4 **未通過** | case list/詳細・承認キュー参照・関係図は実装（#176/#182/#194）。**仕分け UI・名寄せ確認・Q&A/source・OCR 全文保存が未実装** |
| Phase 5 成果物生成 | 28h | **PARTIAL** | G5 **未通過** | 条項ライブラリ（#172）・職務上請求 plan（#196/#197）・財産目録 S3 は実在。**6対象のうち協議書・委任契約・請求書領収書は未着手**、ArtifactVersion 未実装 |
| Phase 6 承認・対外送信 | 34h | **NOT_STARTED** | G6-CORE **未通過** | ApprovalSnapshot／OutboxJob／IntegrationAttempt／ExternalDelivery／StepUpChallenge の実装が repo に不在（設計 draft への言及のみ） |
| Phase 7 有機的統合 pilot | 8h | **NOT_STARTED** | G7 **未通過** | 前提の Phase 4/5/6 が未完。Drive ADR の記録なし |
| Phase 8 安定化・訂正・復旧 | 20h | **NOT_STARTED** | G8 **未通過** | Constitutional Regression Suite・incident drill・backup/restore 実証の記録なし（個別の failure injection test は存在） |
| G8.5 業者 penetration test | 別枠8〜20h | **NOT_STARTED** | G8.5 **未通過** | O-36（業者・NDA・scope 確定）の記録が repo に不在 |
| Phase 9 段階 release・本番受入 | 8h | **NOT_STARTED** | G9 **未通過** | Release A〜D の candidate 固定記録なし |

**全 suite 実測（本監査で実行・2026-08-15）**: `python -m pytest -q --ignore=test_triage_classification.py` → **§1.0a に実出力**。

### 1.0a テスト実測（本監査で実行）

- 実行: `python -m pytest -q --ignore=test_triage_classification.py`（基準 SHA `9406ca6`＝main `f1b9dad` の code に docs 追加のみ・code 無変更）
- 実出力（末尾）:

```
2211 passed, 6 warnings, 1235 subtests passed in 225.93s (0:03:45)
[exited with code 0]
```

- 系列: 1,072（Phase 0 baseline・2026-07-11）→ 1,458（Phase 1 close・2026-07-28）→ 1,937（2026-08-09 台帳）→ 2,073（2026-08-11 台帳 §1b）→ **2,211（2026-08-15 実測）**。単調増加・FAIL 0。
- ① L3357（G8 条件）「主要journeyのE2E・全回帰がbaseline以上でPASSし、test削除・skip・緩和0」のうち**回帰の単調増加は満たしている**（G8 のその他条件は §1.8 のとおり未達）。
- 除外した `test_triage_classification.py` は Claude API 実呼び出しのため（実行には `railway run` が必要）。

### 1.1 Phase 0（現況固定・immediate containment）— PARTIAL／G0 未検証

**成果物11点の実測**:

| ①の成果物 | 判定 | 根拠 |
|---|---|---|
| CURRENT_STATE_REPORT | DONE | `docs/current-state-report.md`（212行） |
| P0_P16_GATE_TABLE | **NOT_STARTED** | repo 全文検索でヒットは①本文のみ。P0〜P16 の状態表が repo に不在 |
| BASELINE_TEST_REPORT | PARTIAL | 数値は `docs/work-logs/2026-07-11_Phase0クローズ_Phase1着手.md` §1 に「1,072 passed / FAIL 0」と記録。ただし同§が機械可読証拠として挙げる `docs/work-logs/2026-07-11_phase0a_baseline.txt` は **repo に不在**（`ls docs/work-logs/*.txt` = 0件） |
| DESIGN_DOC_INDEX | **NOT_STARTED** | ヒットなし（`docs/architecture/README.md` 等は個別 README であり全体 index ではない） |
| 論理名とrepo実名のmapping | **NOT_STARTED** | ヒットなし。① L385「role名は論理名である。現在の実装名はGate 0でmappingする」の対応物が不在 |
| ENDPOINT_TRUST_BOUNDARY_INVENTORY | DONE | `docs/evidence/ENDPOINT_TRUST_BOUNDARY_INVENTORY.md` |
| KINTONE_SCHEMA_AND_TOKEN_MATRIX | PARTIAL | `docs/evidence/KINTONE_TOKEN_MATRIX.md` は実在。**schema export 部分**（① P13「最新schema export」）に相当する文書は不在 |
| GAS_WATCHER_RAILWAY_FINGERPRINT | **NOT_STARTED** | ヒットなし。近いものに `test_p2_gas_drift_tool.py`（GAS drift 検査ツール）があるが fingerprint 台帳ではない |
| SECRET_PII_EXPOSURE_REPORT | DONE | `docs/evidence/SECRET_PII_EXPOSURE_REPORT.md` |
| LEGACY_MIGRATION_LEDGER | **NOT_STARTED** | `docs/automation-task-ledger.md` は自動化タスク台帳であり、①が求める legacy migration の MigrationItem 台帳ではない（MigrationItem・expires_at・owner の列を持たない） |
| 210時間再見積り | **NOT_STARTED** | ヒットなし |

**G0 条件の個別判定**（① L3044〜L3056）:

| G0 条件 | 判定 | 根拠 |
|---|---|---|
| main SHA・dirty・production deploy SHA・全test実出力が固定 | PARTIAL | main SHA・test 数は work-log に記録。**production deploy SHA の記録は repo に不在** |
| RV-01〜RV-14 を再検査し OPEN／CONTAINED／FIXED／STALE 分類 | **NOT_STARTED** | 14件を4分類した表が repo に不在（個別対応は §1.2 のとおり進行しているが、①が求める分類表そのものが無い） |
| public endpoint・webhook署名・query token caller・PII log の影響範囲を実物固定 | DONE | `docs/evidence/ENDPOINT_TRUST_BOUNDARY_INVENTORY.md`・`SECRET_PII_EXPOSURE_REPORT.md` |
| kintone App 21〜38 schema・token権限・GAS deploy hash／trigger・watcher version／heartbeat を取得 | PARTIAL | token 権限=`docs/evidence/KINTONE_TOKEN_MATRIX.md`。**schema export・GAS deploy hash・watcher version は不在** |
| /scan・/ocr/fixed-asset・channel token fallback・Stripe／CloudSign の危険経路が停止または隔離 | **未通過** | CloudSign=FIXED（PR #97）・Stripe=journal 稼働。**/scan・/ocr/fixed-asset は停止も隔離もされていない**（`hub/service_auth.py:578`「現行=無認証受理」）。Phase 0 work-log §2 は「Phase 1 へ（GAS堅牢化→認証の順）」と明記＝G0 時点で意図的に未達のまま先送り |
| P1〜P4 の有無 | **判定不能** | [人]受領物の受領記録が repo に不在。§2 参照 |
| 警報・既知障害・secret／PII 露出なし、または rotation・containment 済み | PARTIAL | rotation 2系統完了（Phase 1 報告 §6）。RCF-M14（Vision billing）は §7(iii) に「別裁定」として残置 |
| 大野が150万円相当のConsole上限・auto reload・警報・Codex課金方式・上限到達runbookを確認 | **判定不能** | repo 外（Console 設定・[人]の確認記録） |
| 文書矛盾一覧 | PARTIAL | `docs/plan/README.md` §5 に③と①②の食い違い10件を列挙（2026-08-13・本監査の前票）。ただし①が想定する repo 全体の文書矛盾一覧ではない |
| Codex R0-A／R0-B 完了・Fable裁定記録済み・未containmentのBLOCKER／HIGH 0 | PARTIAL | R0A-B01/B02/B03・H04・B05 の裁定が Phase 0 work-log §2 に記録。**未containment の BLOCKER が 0 ではない**（/scan・/ocr/fixed-asset の2件が未 containment のまま Phase 1 送り） |

→ **Phase 0 = PARTIAL、G0 = 未検証**（work-log は「Phase 0 実質クローズ（G0）」と記録するが、①の G0 条件のうち少なくとも「危険経路の停止または隔離」が明示的に未達であり、成果物11点中5点が不在）。

### 1.2 Phase 1（統合設計・既存基盤hardening）— PARTIAL／G1 未通過

**repo の自己申告**: `docs/reports/2026-07_phase1-close-report.md` §11「**Phase 1 完全クローズ日 = 2026-07-27**」。ただし同報告のスコープは「顧客 Bot 中核の可観測性・耐障害性の底上げ」「GAS/kintone 連携の認証堅牢化」であり、①の Phase 1 スコープ（① L3060〜L3111）とは一致しない。以下は**①の記述に対する**判定である。

**RV-01〜RV-14 の充足（①L135〜L148 の TO-BE／Phase 欄に対して）**:

| RV | ①のTO-BE | 判定 | 根拠 |
|---|---|---|---|
| RV-01 | G0でSHA・deploy・全test実出力を固定 | PARTIAL | main SHA・test 数は固定。production deploy SHA の記録が不在 |
| **RV-02** | Phase 0で外部到達性を止め、Phase 1で安全なingestへ統合 | **PARTIAL（未達）** | 外部到達性は**止まっていない**。RV-0102-PREP（PR #200）で署名 opt-in の**事前配線のみ**実施。flag OFF 既定・署名ヘッダ皆無なら受理継続（`hub/service_auth.py:575-591`）。§5 参照 |
| **RV-03** | watcherを/valuation/ingestへ移行し、legacy endpointを無効化 | **PARTIAL（未達）** | `/valuation/ingest` は実在（`valuation_ingest.py`・S4-M2）。ただし `VALUATION_INGEST_TOKEN` 未投入で実機未検証（台帳 §3 #2・#7）。legacy `/ocr/fixed-asset` は無効化されておらず `signed_optin_router` 配下で生存（`main.py:1187`） |
| RV-04 | header署名方式へdual-accept移行後、query方式停止・rotation | **DONE** | RV-04a/b/c（PR #127・#128・#137・#138）。sortation lane 本番署名化・`SERVICE_AUTH_LEGACY_DISABLED_PATHS` による段階停止・token rotation 2系統完了（Phase 1 報告 §6・§7(i) 2026-07-27 完了） |
| RV-05 | InboundEvent＋durable workerへ置換 | **DONE** | PR #131（RV-05-13）・`hub/inbound_event.py`・`hub/durable_inbound.py`。**点火完了**＝`INBOUND_EVENT_DURABLE_ENABLED=1`（2026-08-11・`docs/work-logs/2026-08-11_block-a-ignition.md`） |
| RV-06 | durable conversation stateまたは明示的再構成へ移行 | PARTIAL | ConversationSession/PendingCommand に相当する durable 化は `hub/durable_inbound.py` 系で進行。① L3100 が求める「restart／event replay／期限切れ／複数user混線test を PASS」の全通記録は repo に不在 |
| RV-07 | 署名・event journal・read-after-write・reconciliation | **DONE** | `test_inbound_event_stripe.py`・`test_stripe_journal_hardening.py`・`docs/runbooks/stripe-journal-recovery.md`。flag `STRIPE_EVENT_JOURNAL_ENABLED=1` 投入済み |
| **RV-08** | soft merge／unmerge可能なlineageへ段階移行 | **DONE（実装）／点火は[人]** | 設計 FROZEN（`DRAFT_RV08_SOFT_MERGE.md`・D8 PASS）→ 実装 **PR #205**（2026-08-12 merge）。`hub/person_merge_journal.py`・`person_restore_cli.py`。①L3105 の「通常tokenのApp 34削除権限除去」は [人]/CU 側の作業で、App34 CU 4フィールドは完了と台帳が記録 |
| RV-09 | fail closed。channel roleを必須にし、default tokenを廃止 | **DONE** | `hub/notify.py:31-95`（`business_token_env()` 一点集約・allowlist 外は送信 skip）。`test_notify_channel_policy.py` |
| RV-10 | notification最小化、redaction、既存logの露出調査・retention裁定 | PARTIAL | redaction 契約の AST 強制（PR #110・#120・`hub/redact.py`・`redaction_sink_allowlist.json`・台帳61件）は DONE。**「過去PII logの保存先・期間・閲覧者・削除可否・incident該当性を大野が裁定」（①L3102）の記録が repo に不在**＝判定不能 |
| RV-11 | fail closed、署名／event journal、非同期照合、secret移行 | **DONE** | PR #97（CloudSign fail-open 修正・本番反映）・`test_cloudsign_fail_closed.py` |
| RV-12 | lock／hash、SBOM、CI、dependency scan を release gate 化 | PARTIAL | `requirements.lock`・`sbom/`（CycloneDX 1.6）・`docs/runbooks/dependency-lock-sbom.md`（PR #135）。**release gate 化（CI 実行証拠の release record 保存）は Phase 8 条件で未達** |
| RV-13 | IngestionReceipt、TaskBinding、retry／quarantine、read-after-write | PARTIAL | `hub/ingestion_receipt.py`・`test_ingestion_receipt.py`（PR #131 で RV-05 と同時）。**TaskBinding は未実装**（①Phase 3 成果物） |
| RV-14 | liveness／readiness分離、scheduler lease、dead-man monitor | **DONE** | `hub/health_deps.py`（PR #150・#175）・`hub/scheduler.py`・`hub/notify_heartbeat.py`（RCF-M08 対応）・`daily_healthcheck.py` |

**Phase 1 のその他成果物**:

| ①の成果物 | 判定 | 根拠 |
|---|---|---|
| PRODUCT_BRIEF／USER_ROLE_PERMISSION_MATRIX／DOMAIN_WORKFLOW_MAP／SOURCE_OF_TRUTH_MATRIX／NON_FUNCTIONAL_REQUIREMENTS／ERROR_TAXONOMY／REQUIREMENT_TRACEABILITY_MATRIX／APP_SOURCE_OF_TRUTH_AND_WRITE_OWNER_MATRIX／TASK_BINDING_AND_STATE_MAPPING／DOCUMENT_LINEAGE_AND_INVALIDATION_MATRIX／EFFECT_LEVEL_AND_COMMAND_ARBITRATION／PRESERVE_HARDEN_REPLACE_DEFER_LEDGER | **NOT_STARTED**（12点すべて） | repo 全文検索でヒットは①本文のみ |
| App 36 DerivationRun／App 37割付／template registry 設計 | **DONE** | `DRAFT_APP36_DERIVATION_APP37_TEMPLATE_REGISTRY.md`・P3-001（PR #165・#184）・P3-002（PR #164）・`hub/derivation_models.py`・`hub/template_registry.py`。**migration 2本は本番適用済み**（`e7a3c9d2b5f1`・2026-08-11） |
| case_hintへCaseBindingDecision結合／AUTO_BOUND_UNREVIEWED は R3 で停止 | PARTIAL | 封筒・関所・projection（P3-003a/b/CMD・PR #168/#186/#187）は実装済み。flag `HEIR_DERIVATION_ENABLED` 既定 OFF・点火は[人]ゲート（ブロックE 未消化） |
| R4-2 soft merge／unmerge migration 設計 | DONE | 上記 RV-08 |

**G1 条件のうち明確に未通過のもの**:

- ① L3097「**/scanと/ocr/fixed-assetは全callerをsafe ingestへ移して無効化**」→ **未通過**（§5）
- ① L3095「productionの全GAS／watcher callerが新認証へ移行し、旧query token requestを拒否」→ **未通過**。sortation lane は完了だが、**残 lane（koseki/registry/bank/valuation）は G-L3-0 裁定待ち**（台帳 §2a・`SIGNED_LANES` 残3 lane）
- ① L3089「大野が『毎日最初に見るもの』『事務員へ任せる範囲』『弁護士留保』を承認」→ **判定不能**（承認記録が repo に不在）
- ① L3103「direct／transitive dependency と runtime を version／hash 固定し reproducible install と初回SBOM／scan を実証」→ PARTIAL（lock/SBOM は実在、reproducible install の実証記録が不在）

→ **Phase 1 = PARTIAL、G1 = 未通過**。

### 1.3 Phase 2（UX・prototype）— NOT_STARTED／G2 未通過

①の成果物7点（INFORMATION_ARCHITECTURE・NAVIGATION_AND_DEEP_LINK_SPEC・SCREEN_STATE_SPEC・Figma操作prototype 6 journey・work-log内UX acceptance節・20件のUX_TEST_RECORD・design token／component inventory）は**いずれも repo に存在しない**（全文検索でヒットは①本文のみ）。

- G2 条件「大野が第16.1の6 journeyを完走し、PASS／CHANGE／BLOCKEDをwork-logへ記録」→ **未通過**（該当 work-log なし）
- 関連する[人]待ち: 台帳 §2a「**scan 20 件到着**（大野提供サンプル・UX 設計素材）→ P4-003」＝ [人]待ちで停止中

**注記**: ① Phase 2 は UX・prototype であるが、repo の「P2 系列」は残 lane 署名切替であり**別物**（§0.4）。repo に P2 系列の作業実績があることは Phase 2 の充足を意味しない。

### 1.4 Phase 3（PWA control plane）— PARTIAL／G3 未通過

| ①の実装項目 | 判定 | 根拠 |
|---|---|---|
| passkey、session、step-up、revoke | **PARTIAL（passkey 未実装）** | 認証は実装済みだが方式が異なる。`hub/webapp_auth.py:4-6`「認証=パスワード＋署名付き session cookie（DRAFT_P4_PWA_INVENTORY §3 の推奨 (b) 採用）」＝**2026-07-27 の[人]裁定**。session=7日・失効手段=`WEBAPP_SESSION_SECRET` 差し替え。**step-up（書き込み直前の再認証）は未実装** |
| 招待、role変更、suspend／revoke、credential／session失効、task回収 | **NOT_STARTED** | user 管理 UI・role 変更経路が repo に不在 |
| server role／将来case access境界 | **NOT_STARTED** | role 列・case access 検査の実装なし |
| Access前段とorigin迂回防止 | **判定不能** | Cloudflare Access 等は repo 外（production 設定）。repo 側には該当設定なし |
| security headers、CSRF、rate limit、correlation ID | PARTIAL | login 試行制限は実装（`hub/webapp_auth.py` 固定窓カウンタ）。security headers／CSRF／correlation ID の実装は未確認＝**判定不能**（実装箇所を特定できず） |
| audit | **NOT_STARTED** | AuditEvent テーブル・記録経路が repo に不在 |
| durable DB migration・backup／restore | PARTIAL | migration 基盤は DONE（`alembic/`・本番適用済み `e7a3c9d2b5f1`・`f3d8c1a4e9b2`）。**backup／restore の実証記録が repo に不在** |
| InboundEvent、OperationJournal、TaskBinding、projection rebuild | PARTIAL | InboundEvent=DONE（`hub/inbound_event.py`）。projection=`hub/heir_projection.py`・`projection_log`（migration `f3d8c1a4e9b2` 本番適用済み）。**OperationJournal・TaskBinding は未実装** |
| optimistic concurrency | PARTIAL | CANCEL 実装に write-set/preimage・三値判定あり（`hub/heir_cancel.py`・PR #207）。PWA 画面側の stale write 拒否は未実装 |
| manifest、Service Worker | **DONE** | `webapp/manifest.json`・`webapp/sw.js` |
| responsive shell、共通state component | PARTIAL | `webapp/index.html`・`app.js`・画面5枚（login/cases/case/approvals/kinship） |
| environments、feature flag、rollback | PARTIAL | feature flag は多数実在（`HEIR_DERIVATION_ENABLED`・`HEIR_CANCEL_ENABLED`・`INBOUND_EVENT_DURABLE_ENABLED` 等）。**staging 第2環境（① P11A）が不在** |
| structured log、health、monitor hook | **DONE** | `hub/logging_setup.py`・`hub/health_deps.py`・`daily_healthcheck.py` |
| ScheduledRun（schedule key、lease、catch-up policy） | PARTIAL | `hub/scheduler.py` は実在。①が列挙する全 field（intended_run_at・unique・attempt・completed_at・catch-up policy）の充足は未確認＝判定不能 |

**G3 条件のうち明確に未通過のもの**: 「STAGING_READY_CORE 環境で passkey 登録／login／step-up／revoke／origin遮断を全通」「iPhone staging login」「hardware security key 2本＋予備端末を登録し recovery を staging 実証」（① L3156-3158）→ **passkey 自体が未実装のため未通過**。

→ **Phase 3 = PARTIAL、G3 = 未通過**。

### 1.5 Phase 4（PWA中核・既存adapter統合）— PARTIAL／G4 未通過

| ①の実装項目 | 判定 | 根拠 |
|---|---|---|
| 今日／work queue | **NOT_STARTED** | 該当画面なし |
| case list／dashboard | **DONE** | P4-002（PR #176）・`hub/webapp_case_views.py`・`webapp/cases.html`・`case.html`（read-only proxy API＋画面2枚） |
| people／assets／documents／timeline | PARTIAL | 関係図（kinship）は P4-005（PR #194）・`hub/webapp_kinship_view.py`・`webapp/kinship.html`。documents／timeline は未実装 |
| **仕分け** | **NOT_STARTED** | P4-003 は台帳 §2a で「**scan 20 件到着**待ち＝[人]待ち」 |
| 名寄せ確認 | **NOT_STARTED** | PWA 画面なし（LINE 側の関所は既存） |
| Q&A／source | **NOT_STARTED** | §3 の項目10 参照 |
| 新規書類のOCR全文保存 | **NOT_STARTED** | `ocr_text` は読解への一時受け渡しのみで永続化されていない（`bank_ingest.py:241-246`・`config.py:712` 等）。台帳 §2「OCR 2回問題の改善…未着手」 |
| 結果不明・failure | PARTIAL | PENDING_RETRY／PENDING_RECONCILIATION 語彙は ingest 側に存在。PWA 画面での可視化は未実装 |
| LINE deep link／ExternalIdentityLink／LINE identity bootstrap | **NOT_STARTED** | 実装痕跡なし |
| **Q専用LINE account、read-only connector、QA専用append writer** | **NOT_STARTED** | 通知チャネルは顧客Bot（`LINE_CHANNEL_ACCESS_TOKEN`）と業務Bot（`DISPATCHBOT_CHANNEL_ACCESS_TOKEN`）の**2系統のみ**（`hub/notify.py:31-95`）。第3の Q 専用チャネルなし |
| App 29／30／38 の集約readとsource deep link | PARTIAL | 承認キュー参照 P4-004（PR #182・`hub/webapp_approval_view.py`）は「参照のみ・App30 絶対制約と非干渉」（台帳 §1a） |
| 既存LINE／PWA共通Application Service | **NOT_STARTED** | 共通 use case contract の実装なし |

**G4 条件**: 「仕分け実機20件」「通常3tap目標を測定」（① L3199-3200）→ **未通過**（仕分け UI 自体が未実装）。

→ **Phase 4 = PARTIAL、G4 = 未通過**。

### 1.6 Phase 5（成果物生成）— PARTIAL／G5 未通過

**6対象の実測**:

| ①の対象 | 判定 | 根拠 |
|---|---|---|
| 1 職務上請求書 | **PARTIAL** | SHOKUMU-PLAN（設計 FROZEN `DRAFT_SHOKUMU_PLAN.md`・R-SHOKUMU-PLAN-D9 PASS 2026-08-11／実装 PR #196・#197・`hub/shokumu_plan.py`）＝①L3011「koseki_chain／F5→App 36等」の判定起票部分。既存 M1（`channels/shokumu_seikyu.py`）へ委譲する建付け。**FB1（供給方法）は DEFER 裁定**（台帳 §1b） |
| 2 相続放棄 | **PARTIAL** | 順位エンジン `heir_derivation.py`・封筒/関所/projection（P3 系）・取消（PR #207）まで到達。**申述書・代理人目録の生成は未実装**（`docx_templates/souzoku/` は財産目録のみ） |
| 3 財産目録4種 | **PARTIAL** | S3 相当は実在（`units/souzoku/zaisan_mokuroku.py`・`docx_templates/souzoku/財産目録.docx`・2026-07-05 `2c378be`）。**4写像への拡張は未実装**。前提 P4（大野の現行 Word 実物）が未充足（§2） |
| 4 遺産分割協議書 | **NOT_STARTED**（条項ライブラリのみ DONE） | `hub/clause_library.py`・`clauses/v1/iso_kyogi.yaml`（P5-001・PR #172）。**P5-002 以降は「協議書 Word 書式の受入（[人]）待ち」**（台帳 §2a） |
| 5 委任契約書 | **NOT_STARTED** | 実装痕跡なし。前提 P2 未充足 |
| 6 請求書・領収書 | **NOT_STARTED** | 実装痕跡なし。① L3251-3253 の Stripe 条件（raw body 署名検証・event ID unique・二重発行0）のうち Stripe journal 側は実在するが、請求書／領収書生成は未着手 |

**規律・成果物の実測**: 「ArtifactVersion・source revision・template version・bytes hash を Phase 5 で実装する」（① L3233）→ **TemplateVersion registry は DONE**（`hub/template_registry.py`・PR #164）だが **ArtifactVersion・ArtifactDependency・SOURCE_OUTDATED は未実装**。

→ **Phase 5 = PARTIAL、G5 = 未通過**。

### 1.7 Phase 6（承認・対外送信）— NOT_STARTED／G6-CORE 未通過

- ApprovalSnapshot／ApprovalRequest／canonical hash／StepUpChallenge／OutboxJob／IntegrationAttempt／ExternalDelivery／durable worker process／connector別kill switch — **いずれも実装が repo に不在**（`DRAFT_P3_CONTROL_PLANE_INVENTORY.md` に設計語彙としての言及のみ）
- 既存の `hub/approval.py` は **App 30 の legacy 承認状態機械**であり、①の新 ApprovalRequest ではない。同ファイル冒頭に「承認待ち → 承認済への遷移を行うコードパスをサーバー側に作らない。承認は弁護士の kintone 操作のみ」の絶対制約が明記されている
- connector 3種の状態: CloudSign=旧 webhook を fail-closed 化（PR #97）済みだが新 route 未実装／FAX=未着手（設計 `docs/architecture/06-module-03-fax.md`・`06a-fax-provider.md` のみ）／e内容証明=未着手（設計 `05-module-02-enaishomei-csv.md` のみ）
- ① L3309 の状態語彙（MOCK_READY 等）の記録が repo に不在

→ **Phase 6 = NOT_STARTED、G6-CORE = 未通過**。

### 1.8 Phase 7〜9・G8.5 — NOT_STARTED／すべて未通過

- **Phase 7**: 全通 pilot の evidence index が不在。Drive ADR（KEEP_CURRENT／PILOT／DEFERRED）の裁定記録も不在 → NOT_STARTED／G7 未通過
- **Phase 8**: Constitutional Regression Suite・incident drill・backup→restore 実証・外部有料 uptime 監視の記録が不在。個別の failure injection test（`test_rv05_13_durable.py` 等）は実在するが①が求める全 E2E ではない → NOT_STARTED／G8 未通過
- **G8.5**: O-36（業者・NDA・sub-gate scope・test期間・停止条件）の確定記録が repo に不在 → NOT_STARTED／未通過
- **Phase 9**: Release A〜D candidate の固定記録・release record が不在 → NOT_STARTED／G9 未通過

---

## 2. ①の P0〜P16 準備 Gate（17件）の充足実測

①L2969〜L2992 の表に対する実測。**[人]受領物の受領記録は repo に無いため「判定不能」が多い**——これは①L2992「READY／TO_COLLECT／IN_PROGRESS／BLOCKED／NOT_APPLICABLE／STALE を Phase 0 で実物により判定する」の対応物（P0_P16_GATE_TABLE）が repo に不在であることの帰結でもある（§1.1）。

| ID | ①の必要物 | 判定 | 根拠 |
|---|---|---|---|
| P0 | 現在main SHA、git status、最新work-log、baseline test | **DONE** | `2026-07-11_Phase0クローズ_Phase1着手.md` §1（SHA `87a4938`・1,072 passed）。ただし機械可読証拠 `.txt` は不在（§1.1） |
| P1 | 職務上請求書の現行template実物 | **判定不能** | M1 は 2様式重ね打ちで稼働（`channels/shokumu_seikyu.py`・`test_shokumu_form.py`）＝様式資産は存在。ただし①が言う「現行template実物」の**受領記録**が repo に無く、既存資産で充足したのか未受領なのか確定できない |
| P2 | 類型別委任契約書の現行template | **NOT_STARTED（未受領）** | `docx_templates/` に委任契約書なし（`送付状_委任契約書.docx` は送付状）。項目5 未着手と整合 |
| P3 | 請求書・領収書の現行書式と採番rule | **NOT_STARTED（未受領）** | 該当 template・採番 rule の実装なし |
| P4 | 財産目録の現行Word実物1枚以上 | **NOT_STARTED（未受領）** | `docx_templates/souzoku/財産目録.docx` は存在するが、これは PC-A 生成物（`make_zaisan_mokuroku_template.py`・追加 commit `2c378be` 2026-07-05「S3 財産目録の生成」）であり**大野の現行実物ではない**。③L261 も「7/7未回収の宿題」と記載 |
| P5 | CloudSign契約planのAPI利用可否・公式仕様 | **判定不能** | repo 外（契約情報）。repo 側に API 可否の裁定記録なし |
| P6 | 業務類型×顧客の分類表 | **NOT_STARTED（未受領）** | 該当データなし |
| P7 | 熊澤案件等の許可済み実機素材 | PARTIAL | 熊澤案件の実機投入実績は 7月に多数（台帳 §1「実機で熊澤5名を App 34 に生成済み」）。**従前戸籍（入間・熊澤博）は未取得**（台帳 §2「取得待ち」） |
| P8 | LINE仕分けの不満点メモ＋scan20件 | **NOT_STARTED（未受領）** | 台帳 §2a「scan 20 件到着…[人]待ち」 |
| P9 | InterFAX正式test契約、API資料、sandbox、許可済みtest宛先 | **判定不能** | repo 外（契約）。設計 `docs/architecture/06a-fax-provider.md` はあるが契約状態の記録なし |
| P10 | e内容証明公式雛形ZIP・現行実務書面 | **NOT_STARTED（未受領）** | repo に雛形 ZIP・実務書面なし |
| P11A | Railway staging第2環境、staging DB／domain／secret、kintone test space、Drive test folder、test LINE、synthetic fixture | **NOT_STARTED** | staging 第2環境の記録が repo に不在（railway link は production 単一・`creative-manifestation`／`production`）。synthetic fixture は test 内に存在 |
| P11B | production固定domain／RP ID、HTTPS、Cloudflare有料plan、iPhone、security key 2本、予備端末 | **NOT_STARTED** | RP ID・Cloudflare 設定の記録なし。passkey 未実装と整合 |
| P12 | CloudSign／FAX／e内容証明の正式test契約、sandbox、許可済みtest宛先 | **判定不能** | repo 外（契約） |
| P13 | kintone関連Appの最新schema export、status、token権限表 | **PARTIAL** | token 権限表=`docs/evidence/KINTONE_TOKEN_MATRIX.md`・App 定義=`config.py:111-`・CU 指示書 10本（`docs/instructions/cu-app*.md`）。**schema export 実物は不在** |
| P14 | GAS 4本、watcher、Railway deploy／configのfingerprint | **PARTIAL** | GAS 正本同期＝`legacy/gas`・drift 検査ツール `test_p2_gas_drift_tool.py`。**fingerprint 台帳は不在** |
| P15 | 匿名化した代表PDF／戸籍／財産／期待結果corpus | **PARTIAL** | 期待結果 corpus は実在（凍結47ケース＝`docs/koseki-ocr/09-heir-test-cases.md`・`test_p3_001_frozen_cases.py`）。代表 PDF の匿名化 corpus は repo に不在 |
| P16 | Q&A、audit、thumbnail、外部証跡、logのretention方針 | **NOT_STARTED** | retention 方針文書が repo に不在（① L3102 の「過去PII logの…大野が裁定」も未記録） |

**要約**（①が P11 を P11A／P11B に分けているため、ID 数は 17、行数は 18):

| 分類 | 件数 | ID |
|---|---|---|
| DONE | 1 | P0 |
| PARTIAL | 4 | P7・P13・P14・P15 |
| NOT_STARTED | 9 | P2・P3・P4・P6・P8・P10・P11A・P11B・P16 |
| 判定不能 | 4 | P1・P5・P9・P12 |

（合計 1+4+9+4=18 行）

**要点**: ①が「人から受領すべき」と明記する **P1〜P12／P15／P16 のうち、受領が実物で確認できたものは 0 件**である（P7 のみ実機素材の一部が過去に投入済みで PARTIAL）。①L2981 の P8（scan20件）と、③L261 の P4（財産目録の現行 Word 実物）は、③作成時点（2026-07-08。P4 は「7/7未回収の宿題」）から**約5週間、未回収のまま**であり、Phase 2（UX）・Phase 4（仕分け UI）・Phase 5（財産目録4写像）を同時に止めている。

---

## 3. 成果物インベントリ（③の項目1〜13＋PWA）

各項目が①のどの Phase に対応するかは、**①L2949「17.0 元13項目との対応」表の原文引用**による。

| ③の項目 | ①の対応記述（原文引用） | 実装状況 | 根拠 |
|---|---|---|---|
| 1 職務上請求書 | 「\| 1 職務上請求書 \| Phase 5 \|」 | **PARTIAL** | 判定起票=`hub/shokumu_plan.py`（PR #196・#197・設計 FROZEN）／生成・発送=既存 M1（`channels/shokumu_seikyu.py`）。FB1 は DEFER |
| 2 相続放棄 | 「\| 2 相続放棄 \| Phase 5 \|」 | **PARTIAL** | 順位エンジン `heir_derivation.py`・P3 系（封筒/関所/projection/取消・PR #168/#186/#187/#191/#193/#207）。申述書・代理人目録の生成は未実装。flag `HEIR_DERIVATION_ENABLED` 既定 OFF |
| 3 遺産分割協議書 | 「\| 3 遺産分割協議書 \| Phase 5 \|」 | **DESIGN_ONLY** | 条項ライブラリのみ（`hub/clause_library.py`・`clauses/v1/iso_kyogi.yaml`・PR #172）。P5-002 以降は[人]書式受入待ち |
| 4 財産目録4種 | 「\| 4 財産目録4種 \| Phase 5 \|」 | **PARTIAL** | 汎用1種は稼働（`units/souzoku/zaisan_mokuroku.py`）。4写像は未実装。P4 未受領 |
| 5 委任契約書 | 「\| 5 委任契約書 \| Phase 5 \|」 | **NOT_STARTED** | 実装・template とも不在 |
| 6 請求書・領収書 | 「\| 6 請求書・領収書 \| Phase 5 \|」 | **NOT_STARTED** | 実装不在（Stripe journal 基盤のみ既存） |
| 7 CloudSign | 「\| 7 CloudSign \| Phase 6 \|」 | **NOT_STARTED**（封じ込めのみ DONE） | 旧 webhook fail-closed 化（PR #97・`test_cloudsign_fail_closed.py`）。新 route・承認送信は未実装 |
| 8 e内容証明 | 「\| 8 e内容証明 \| Phase 6 \|」 | **DESIGN_ONLY** | `docs/architecture/05-module-02-enaishomei-csv.md` のみ。①L3313 で自動化境界は【FIXED】 |
| 9 FAX | 「\| 9 FAX \| Phase 6 \|」 | **DESIGN_ONLY** | `docs/architecture/06-module-03-fax.md`・`06a-fax-provider.md` のみ |
| 10 Q系Bot | 「\| 10 Q系Bot \| Phase 4 \|」 | **NOT_STARTED** | 下表 §3.1 で要件7点を個別判定 |
| 11 Drive二階層 | 「\| 11 Drive二階層 \| Phase 7でADR。KEEP_CURRENTを正規合格とし、必要時だけpilot \|」 | **NOT_STARTED** | ADR・移行設計とも不在。①では「必須成果ではない」（L3319） |
| 12 PWA | 「\| 12 PWA \| Phase 1〜4・6 \|」 | **PARTIAL** | 認証 shell（PR #173）・案件一覧/詳細（#176）・承認キュー参照（#182）・関係図（#194）。仕分け UI・Q&A 一覧は未実装 |
| 13 ラベル実機 | 「\| 13 ラベル実機 \| Phase 8 \|」 | **NOT_STARTED** | 実機テスト記録なし（ラベル生成コードは `channels/` に存在） |

### 3.1 項目10（Q系質問応答Bot）要件7点の個別判定

③L148「要件7点（7/7確定・変更禁止）」に対する実測。

| # | 要件 | 判定 | 根拠 |
|---|---|---|---|
| 1 | 専用LINEアカウント新設（指示Bot＝行動／質問Bot＝参照の分離） | **NOT_STARTED** | 通知チャネルは2系統のみ（`hub/notify.py:31`＝`DISPATCHBOT_CHANNEL_ACCESS_TOKEN`、`hub/notify.py:81`＝`LINE_CHANNEL_ACCESS_TOKEN`）。第3チャネルの env・router なし。①も L3192 で「Q専用LINE account」を Phase 4 実装項目に置く |
| 2 | 読み取り専用（書き込み語彙を一切持たない） | **NOT_STARTED** | Q Bot 自体が不在。なお①L3204「Q専用accountから業務正本write 0」は G4 条件 |
| 3 | 出典明示（レコード番号つき回答） | **NOT_STARTED** | 該当実装なし |
| 4 | 未確定データの注記 | **NOT_STARTED** | 該当実装なし（名寄せ未確定の概念は `hub/person_validity.py` 等に存在するが Q 回答への注記経路は不在） |
| 5 | 第1版はkintone構造化データ＋読解JSON（PDF全文検索は第2版） | **N/A（スコープ定義）** | ①も L3187 で「PDF全文検索UIは作らない」と一致（矛盾なし） |
| 6 | 仕分け時OCRテキストの保存 | **NOT_STARTED** | `ocr_text` は読解への一時受け渡しのみで永続化なし（`bank_ingest.py:241-246`・`config.py:712/773/798`）。台帳 §2「OCR 2回問題の改善…未着手」。①L3187「新規書類のOCR全文保存」も Phase 4 で未着手 |
| 7 | 書類種別ごとの信頼度格付け | **PARTIAL（流用元のみ存在）** | `doc_type_confidence` は仕分けに実在（`sortation_ingest.py:79/102/113/526`）。ただし Q 回答への「活字/印字/手書き・旧字」の格付け適用は未実装 |

---

## 4. ③第6章「7月残タスク8件」の消化状況

①L2996 の「7月残taskの依存判定」表を併記する。

| # | ③のタスク | ①の扱い（原文引用） | 判定 | 根拠 |
|---|---|---|---|---|
| 1 | R4-3b 導出結果→関所→App 36起票 | 「Phase 5項目2 ｜ 前提。Phase 5前に完了」 | **DONE（実装）／点火は[人]** | P3-003a 封筒（PR #168）・P3-003-CMD（#186）・P3-003b 関所+projection（#187）。migration 本番適用済み（`e7a3c9d2b5f1`・2026-08-11）。flag `HEIR_DERIVATION_ENABLED` 既定 OFF＝ブロックE 未点火（台帳 §2a） |
| 2 | R4-3c 本番裁定・CUフィールド追加【人・CU】 | 「Phase 4・5 ｜ 前提。人／CU結果を実物確認」 | **PARTIAL** | App 34 CU（RV08 前提の4フィールド）は完了と記録（`2026-08-12_design-freeze-sprint.md` §1・台帳）。**App 36 の人物ID/導出根拠フィールドの完了記録は repo に不在**＝残部は判定不能 |
| 3 | S6実機（滞留通帳2件） | 「Phase 5項目4 ｜ 実財産data検収の前提かをPhase 0で裁定」 | **NOT_STARTED** | 台帳 §3 #3「`BANK_INGEST_TOKEN` 投入 → …確認」＝未検証在庫のまま。env 投入も未（§3 #7） |
| 4 | No.20/21の親エッジ欠落の回収 | 「Phase 4・5 ｜ 半血判定等に使う前に回収」 | **判定不能** | repo の work-log・台帳に回収記録が見当たらない（kintone 実データ側の作業のため repo からは確認不能） |
| 5 | /scan堅牢化＋氏名待ちフォールスルー | 「Phase 1 safe-ingest migrationへSUPERSEDED ｜ legacy /scanを延命せずcaller移行後disable。Phase 4は新ingest回帰だけ」 | **PARTIAL** | RV-0102-PREP（PR #200）で署名 opt-in 事前配線のみ。**disable も caller 移行も未実施**。§5 で材料を提示 |
| 6 | R4-1改修（現行戸籍在籍者の生存自動設定） | 「Phase 5項目2 ｜ 判定入力に使う前に完了」 | **判定不能** | `heir_derivation.py` に `alive` 概念は実在（L49・L201・L423 等）が、「現行戸籍在籍者の**自動設定**」を行う改修コミットを特定できず。台帳にも消込記録なし |
| 7 | 小粒7件（segments実値ログ・doc_type_confidenceログ・重複排除キー正規化・身分事項行順序・App 30未対応チャネル文言・単独数字返信の解釈・通知渋滞対策） | 「log・文言・表示の小粒 ｜ Phase 8 ｜ 依存性なしなら安定化期のみ」 | **PARTIAL** | MAINT-4（PR #198）で4点完了（App38 死活監視登録・App30「人の確認待ち」文言・担保内容空化・App25 実機追随）＝台帳 §2 の4行消込。**③が挙げる7項目とは一致しない**（③の7項目の個別消込記録は repo に不在） |
| 8 | R5系設計調査・Z3（8月スコープ外・9月候補） | 「R5・Z3 ｜ 9月以降 ｜ DEFERRED。器以上を作らない」 | **NOT_STARTED（意図どおり）** | 台帳 §2 に未着手として登録。①③とも 9月送りで一致 |

---

## 5. D-09 の実測材料（判定はしない・材料の提示のみ）

### 5.1 ①の当該記述と前後文脈（原文引用）

**当該行**（① L3001・「17.0.1 P0〜P16準備Gate」直後の「### 7月残taskの依存判定」表内）:

> | /scan堅牢化・氏名待ちfall-through | Phase 1 safe-ingest migrationへSUPERSEDED | legacy /scanを延命せずcaller移行後disable。Phase 4は新ingest回帰だけ |

**前後文脈**（同表の他行・① L2998〜L3003）:

> | R4-3b 導出結果→関所→App 36 | Phase 5項目2 | 前提。Phase 5前に完了 |
> | R4-3c App 34／36 fieldと本番裁定 | Phase 4・5 | 前提。人／CU結果を実物確認 |
> | No.20／21親people edge | Phase 4・5 | 半血判定等に使う前に回収 |
> （…当該行…）
> | 現行戸籍在籍者の生存自動設定 | Phase 5項目2 | 判定入力に使う前に完了 |
> | S6実機・通帳2件 | Phase 5項目4 | 実財産data検収の前提かをPhase 0で裁定 |

**同一文書内の関連記述**:

- ① L136（RV-02）: 「legacy /scanは認証、durable idempotency、PDF上限がなく、AIのfree-form JSONを使い、抽出dataとkintone requestをlogへ出す ｜ **BLOCKER** ｜ **Phase 0で外部到達性を止め、Phase 1で安全なingestへ統合**」
- ① L137（RV-03）: 「legacy /ocr/fixed-assetは認証がなく…所在部分一致の先頭recordを更新し得る ｜ **BLOCKER** ｜ watcherを/valuation/ingestへ移行し、**legacy endpointを無効化**」
- ① L3097（G1 条件）: 「**/scanと/ocr/fixed-assetは全callerをsafe ingestへ移して無効化**。fixed-asset相当の更新は地番等の完全識別条件を使い、0件／複数件でwrite 0」
- ① L3093（G1 条件・RV-04 側）: 「STAGING_READY_COREで**HMAC dual-accept→new-only**、GAS／watcher caller切替を全通・rollbackしてからproduction cutover」

### 5.2 RV-0102-PREP（PR #200）が実物として何をしたか

**merge**: PR **#200**（`impl-batch-1`・merge commit `adf0dfd`・2026-08-11）。台帳 §1b「RV-0102-PREP（/scan・/ocr/fixed-asset の署名 opt-in 事前配線）｜ #200（IMPL-BATCH-1 A）｜ テスト →2073。**強制化・GAS/watcher 点火は[人]別途**」。

**実装の実体**（`hub/service_auth.py:572-601`・原文引用）:

> ```
> # ── RV-0102-PREP: 旧 token を持たない受信口（/scan・/ocr/fixed-asset）の
> #    署名 opt-in 事前配線（薄い追加・verify_*/authorize_ingest は挙動不変） ──
>
> async def authorize_optionally_signed(request: Request) -> None:
>     """署名 opt-in 入口の dual-accept ゲート（RV-0102-PREP）。
>
>     /scan・/ocr/fixed-asset は旧 query token を持たない（現行=無認証受理）ため、
>     authorize_ingest（token fallback 前提）は使えない。本ゲートの分岐:
>     - flag OFF: 何もしない（署名ヘッダが付いていても無視＝現行挙動と完全同一）
>     - flag ON・署名ヘッダ在: 署名経路のみで判定（§2.3 全8段・token/無認証へ
>       fallback しない＝downgrade 防止。authorize_ingest の署名分岐と同一実体
>       〔_enforce_signed_request〕）
>     - flag ON・署名ヘッダ皆無: 受理（従来どおり＝現行挙動不変）。非署名の遮断
>       （強制化）と送信側の署名付与（GAS/watcher 点火）は[人]ゲートの別票
>     """
> ```

**配線の実体**: 両 endpoint は `signed_optin_router` へ移され（`main.py:16`・`main.py:1187`・`main.py:1474`）、`Depends(optional_signature_guard())` を持つ。

**テスト契約**（`test_rv0102_prep_signed_optin.py:1-13`・原文引用）:

> 両入口は旧 query token を持たない（現行=無認証受理）。dual-accept 事前配線の契約:
> - flag OFF: 署名ヘッダの有無に関わらず現行挙動と完全同一（回帰 pin）
> - flag ON・署名ヘッダ皆無: 従来どおり受理（現行挙動不変・強制化は[人]別票）
> - flag ON・署名ヘッダ在: 署名経路のみで判定——署名不正・期限外・replay（nonce 再使用）・別 path 転用・body 改変・unknown key → 拒否

**flag**: `SERVICE_AUTH_DUAL_ACCEPT_ENABLED`（`test_rv04b_dual_accept.py:64`）。本番投入状況は repo からは**判定不能**。

**Phase 0 時点の先送り理由**（`docs/work-logs/2026-07-11_Phase0クローズ_Phase1着手.md` §2・原文引用）:

> | /scan・/ocr/fixed-asset無認証（R0A-B01/B02） | Phase 1へ（**GAS堅牢化→認証の順**。サーバ側を先に締めるとGAS既存3フォルダループ〔try/catchなし〕が全ライン道連れ停止・P0B-001） |

### 5.3 両解釈が成り立つかの事実提示

**「廃止に向けた封じ込めの一部」と読める事実**:

1. ①が RV-04 について G1 で求める手順は「**HMAC dual-accept→new-only**」（L3093）であり、#200 が実装した dual-accept は**①自身が指定する移行手順の第1段**と同型である。実際、検証器・registry・署名ヘッダ組立は `test_rv04b_dual_accept` と同一物を共用しており（テスト冒頭の明記）、別実装ではない。
2. 「caller移行後disable」（L3001）を実行するには、送信側（GAS/watcher）が署名を付けられる受信側が先に存在する必要がある。#200 はその受信側だけを用意し、**送信側の点火と非署名の遮断（強制化）を[人]ゲートの別票として明示的に切り離している**。
3. #200 は endpoint の機能（PDF 上限・free-form JSON・log 出力等の RV-02 本体の欠陥）を**拡張していない**。追加されたのは認証ゲート1点のみで、コメントにも「薄い追加・verify_*/authorize_ingest は挙動不変」とある。
4. Phase 0 の先送り理由（§5.2 末尾）は「サーバ側を先に締めると GAS 全ライン道連れ停止」であり、dual-accept は**その道連れ停止を避けながら締める**唯一の順序という読み方ができる。

**「延命」と読める事実**:

1. ①の TO-BE は一貫して **disable / 無効化**である（L137「legacy endpointを無効化」・L3097「全callerをsafe ingestへ移して**無効化**」・L3001「**延命せず**caller移行後disable」）。#200 は legacy endpoint を**存続させたまま**認証を足しており、①が指す safe-ingest への caller 移行（`/valuation/ingest` 等の新 ingest への移行）は行っていない。
2. ① RV-02 の Phase 割当は「**Phase 0 で外部到達性を止め**、Phase 1 で安全な ingest へ統合」である。2026-08-15 現在、外部到達性は止まっていない（flag OFF 既定・flag ON でも署名ヘッダ皆無なら受理）。**Phase 0 の containment 要求（RV-02＝BLOCKER）が、2026-07-11 の Phase 0 クローズから約5週間を経てなお未達**という状態に、#200 は変化を与えていない（テストが「flag OFF＝現行挙動と完全同一」を回帰 pin として固定している）。
3. ①L3001 の「Phase 4は新ingest回帰だけ」は、legacy /scan に対する追加作業を Phase 1 の disable 以降**想定していない**書き方である。#200 は legacy /scan に対する新規実装であり、この記述には現れない作業である。
4. 対になる RV-03 の TO-BE「watcher を /valuation/ingest へ移行」については、移行先 `/valuation/ingest` が `VALUATION_INGEST_TOKEN` 未投入で**実機未検証のまま**（台帳 §3 #2・#7）であり、移行の受け皿が動作実証されていない状態で legacy 側に認証を足したことになる。

**両解釈に共通する事実**（どちらの読みでも変わらない）:

- 2026-08-15 現在、`/scan`・`/ocr/fixed-asset` は **無認証で受理される状態が既定**である（`hub/service_auth.py:578`・`test_rv0102_prep_signed_optin.py` の契約）。
- ① の G1 条件 L3097 は**未通過**である。
- 次に必要な作業（強制化・GAS/watcher 点火）は **[人]ゲートの別票**として repo に明記されている。

**PC-A は本節でいずれの解釈も採らない**（司令塔裁定事項）。

---

## 6. 凍結事項との整合実測（8月実装分 × ①②③の凍結事項）

矛盾は**修正せず列挙のみ**。

| # | 凍結事項 | 整合性 | 実測 |
|---|---|---|---|
| F-1 | 料金体系 | **整合** | 時効援用=「44,000円（税込）」（`chat_responder.py:168-189`・「2026-07-03 弁護士確定」の明記あり・`FEE_REQUIRED_PHRASES` で必須語検査）。相続放棄=88,000円＋2人目以降33,000円（`docs/architecture/10-unit-02-souzoku-houki.md:28`）。③L94 が挙げる「標準型77,000円/超過型88,000円〜」も同書 L8-9 に類型別として併記され、旧案（一律88,000円）は不採用と明記。**8月実装分（PR #188〜#207）に料金の変更は無い**（該当ファイル無変更） |
| F-2 | purpose 文言 | **整合** | 職務上請求の purpose は `channels/shokumu_seikyu.py:343-367` が単一の生成点。8月の SHOKUMU-PLAN 実装は `hub/shokumu_plan.py` 冒頭に「M1 既存経路への委譲（**様式・料金・purpose・宛先ロジックの複製禁止**）」と明記し、複製を作っていない |
| F-3 | 47ケース表 | **整合** | `test_p3_001_frozen_cases.py:119-120` が「凍結 47 行（A13+B8+C5+D5+E6+F6+G4）」の件数を機械的に固定。正本=`docs/koseki-ocr/09-heir-test-cases.md`（`git log --since=2026-08-01 -- docs/koseki-ocr/09-heir-test-cases.md` = **0件**＝8月無変更）。8月の P3-003c/CANCEL 実装も同 suite を維持（本監査の全 suite 実測でも PASS） |
| F-4 | H系列正本（`docs/souzoku-houki/`・監修5層） | **整合** | 10ファイル（01-flow〜10-koseki-matrix）が現存。8月の変更なし（`git log` で 8月コミットに `docs/souzoku-houki/**` の変更なし）。①L3012 も「H系列正本＋順位engine＋続柄別戸籍matrix」を項目2の判定正本としており一致 |
| F-5 | 項目10 要件7点 | **矛盾なし（未実装のため）** | §3.1 のとおり全て未実装。要件5（PDF全文検索は第2版）は①L3187「PDF全文検索UIは作らない」と一致。**8月実装分に要件7点を侵す実装は無い** |
| F-6 | ①12章相当のセキュリティ設計 | **【矛盾1件】認証方式** | ①L638「**passwordを主認証にせず**、固定domain上でpasskeyにより本人確認する」／①L310「外部効果のある操作は直前にWebAuthn再認証する」に対し、実装は **password＋署名付き session cookie**（`hub/webapp_auth.py:4-6`）。**step-up（書き込み直前の再認証）も未実装**。ただし同ファイルは根拠として「**裁定（2026-07-27・[人]）**: 認証=パスワード＋署名付き session cookie（DRAFT_P4_PWA_INVENTORY §3 の推奨 (b) 採用）」を明記。この[人]裁定は①②（2026-07-10）より**後**であり、①L108 の TO-BE 規範順位第1位「日付の新しい大野の明示裁定と弁護士承認済み凍結仕様」に該当し得る（**適用の可否は司令塔裁定事項・本監査では判定しない**） |
| F-6b | 同上・session 期限 | **【矛盾1件】** | ③12.5-2「トークンに有効期限（例12時間）」に対し実装は **7日**（`hub/webapp_auth.py`・`SESSION_TTL_SECONDS`）。①側に具体的な期限値の指定は見当たらず（①L2393「session窃取…短期限、失効」のみ）＝③に対する矛盾。③は SUPERSEDED 扱いのため参考記載 |
| F-6c | 同上・ネットワーク層の壁 | **未充足（矛盾ではない）** | ③12.5-3／①L2395「推測不能URLをsecurity controlとして扱わない」＋Access 前段。repo 側に Cloudflare Access 等の設定記録なし＝Phase 3 未達（§1.4）。production 設定は repo 外のため**判定不能** |
| F-7 | 権限の弁護士留保（③12.6・①3章） | **整合** | ①L369「\| 事務員 \| 将来利用を前提に器だけ作る \| 仕分け、内部task、書類確認 \| **対外承認不可** \|」／③L215「対外効果のある承認（e内容証明・FAX・CloudSign送信）は弁護士＝大野に留保」。実装: `ATTORNEY_ALLOWLIST` による確定関所の権限検証（`hub/heir_projection.py:12`・`hub/heir_cancel.py:5,200`・`dispatch_bot/review_resolve_task.py:285`）。取消も「開始は弁護士の明示操作のみ」。さらに `hub/approval.py` 冒頭の**絶対制約**「『承認待ち→承認済』への遷移を行うコードパスをサーバー側に作らない。承認は弁護士の kintone 操作のみ」を `test_hub_approval.py` が恒久担保。**8月実装分（CANCEL・PR #207）もこの線を踏襲**（`ATTORNEY_ALLOWLIST` 外は拒否） |
| F-8 | ②の役割分担（push は人のみ 等） | **整合** | ②の規律に対応する repo/運用側の実物: push は deny で物理遮断（`~/.claude/settings.json` 22件・すり抜け5経路テスト済み＝Phase 0 work-log §1）。8月の全 merge（#188〜#207）は PR 経由。③L291「**pushは人間のみ/本番変更コマンドは`!`有無問わずPC-Aは人に返す/本番書き込みは明示指示のみ**」も同旨。**矛盾なし** |
| F-9 | ①の「機械は確定しない」原則 | **整合** | ①L3201「E2 machine自動確定0」。実装: App36 write 0 原則（P3-003b）・confirmed 一本化・`hub/app36_validity.py`（取消済み=yes 読み飛ばしの単一の正）。8月の CANCEL 実装も supersede 型で「機械が確定を作らない」建付け |

### 6.1 矛盾の要約

**発見した矛盾＝2件**（いずれも同一領域＝PWA 認証）:

1. **F-6: 認証方式**（①=passkey/WebAuthn 必須・step-up 必須 ↔ 実装=password＋session cookie・step-up 無し）。ただし 2026-07-27 の[人]裁定が根拠として実装内に明記されており、①の規範順位第1位（日付の新しい大野の明示裁定）との関係は司令塔裁定事項。
2. **F-6b: session 期限**（③=例12時間 ↔ 実装=7日）。③は SUPERSEDED 扱いのため参考。

**その他は矛盾なし**。F-6c は「未充足」であって矛盾ではない。

**この矛盾が Gate に与える影響（事実のみ）**: ① G3（L3156-3158）は passkey 登録／login／step-up／revoke／hardware security key 2本での recovery 実証を通過条件としているため、現行の認証方式のままでは **G3 は定義上通過できない**。①L3419 は「R8／G8＋G8.5-CORE 合格後: production へ A→B→C を feature flag で順次有効化」としており、G3 未通過は Release A〜C の production 有効化にも連鎖する。

---

## 7. 総括

### 7.1 ①の工程上の現在地

**現在地 = Phase 1 の途中（G1 未通過）。ただし Phase 3・4・5 の実装が先行着手されている状態。**

事実として:

- **Phase 0 は PARTIAL／G0 未検証**。成果物11点中5点が不在、かつ G0 条件「/scan・/ocr/fixed-asset の危険経路が停止または隔離」が**意図的に Phase 1 へ送られたまま未達**（Phase 0 work-log §2）。
- **Phase 1 は PARTIAL／G1 未通過**。RV-04/05/07/09/10/11/12/13/14 は実装・本番反映まで到達（repo は「Phase 1 完全クローズ 2026-07-27」と記録）だが、①の G1 条件のうち少なくとも次が未達:
  - /scan・/ocr/fixed-asset の無効化（L3097）
  - 残 lane（koseki/registry/bank/valuation）の caller 移行（L3095・G-L3-0 裁定待ち）
  - Phase 1 成果物12点（PRODUCT_BRIEF 以下）の全欠落
- **Phase 2 は完全に未着手**（UX・prototype）。①の工程では Phase 3 の前段である。
- 一方で **Phase 3（webapp shell・durable DB・scheduler）・Phase 4（案件一覧/詳細・承認キュー参照・関係図）・Phase 5（条項ライブラリ・職務上請求 plan）に実装が入っている**＝①の Phase 順序に対して**先行着手が発生している**。
- **Phase 6 以降は未着手**。

したがって「210時間版の Phase を順に消化した結果いまここにいる」のではなく、「**Phase 1 の未達条件と Phase 2 を飛ばしたまま、Phase 3〜5 の一部が先に積まれている**」というのが実測上の現在地である。

### 7.2 ①の記述上、次に来るべき作業

以下は**①本文の引用**であり、PC-A の意見ではない。

**(a) Gate の順序について**（① L3111・L3132 等が各 Phase 末尾で共通に要求）:

> Codex R1完了、Fable裁定記録済み、未解決BLOCKER／HIGH 0

**(b) 未達の BLOCKER について**（① L136・RV-02 の Phase 欄）:

> Phase 0で外部到達性を止め、Phase 1で安全なingestへ統合

**(c) G1 の残条件**（① L3097）:

> /scanと/ocr/fixed-assetは全callerをsafe ingestへ移して無効化。fixed-asset相当の更新は地番等の完全識別条件を使い、0件／複数件でwrite 0

**(d) G1 の残条件・caller 移行**（① L3095）:

> productionの全GAS／watcher callerが新認証へ移行し、旧query token requestを拒否、旧token rotation、legacy traffic 0、retirement evidenceを保存

**(e) Phase 1 の位置づけ**（① L3062）:

> このPhaseは計画上最大の工数riskである。…20時間へ収めるためにcutover、retirement evidence、failure test、復元性を省略しない。40時間超予測ならPhase 1着手前に全体を再裁定する。

**(f) Phase 2 の Gate**（① L3127）:

> 大野が第16.1の6 journeyを完走し、PASS／CHANGE／BLOCKEDをwork-logへ記録

**(g) Phase 3 の Gate 冒頭**（① L3156）:

> STAGING_READY_CORE環境でpasskey登録／login／step-up／revoke／origin遮断を全通してからproduction RPへ進む

**(h) 未完の丸め禁止**（① L3430・G9・および L3440）:

> 未完をCOMPLETEに丸めず、PARTIAL／DEFERRED／BLOCKED_EXTERNALと残条件・ownerを記録

> codeがあるだけの外部項目はCOMPLETEにしない。

**(i) 準備 Gate の判定について**（① L2992）:

> READY／TO_COLLECT／IN_PROGRESS／BLOCKED／NOT_APPLICABLE／STALEをPhase 0で実物により判定する。「あるはず」をREADYとしない。

### 7.3 本監査が示す構造的事実（事実の指摘のみ・裁定は司令塔）

1. ①の Phase 0 成果物のうち **P0_P16_GATE_TABLE が不在**であることが、§2 で「判定不能」が4件・「未受領」が9件になった直接の原因である（①L2992 が要求する実物判定が一度も行われていない）。
2. ①の G0 が要求する **RV-01〜RV-14 の4分類表が不在**のまま個別対応が進んだため、「どの BLOCKER が CONTAINED でどれが OPEN か」を repo から一意に読めない。本監査は §1.2 でこれを RV 単位に再構成した。
3. **repo の P 系列番号と①の Phase 番号のずれ**（§0.4）により、「Phase 1 完全クローズ」等の既存記録を①の Gate 通過として読むと過大評価になる。

---

（本監査は READ_ONLY で実施し、コード・既存 docs への変更は行っていない。判定の根拠はすべて基準 SHA `9406ca6` 時点の repo 実物である。矛盾の修正・工程の裁定は行っていない＝司令塔裁定事項）
