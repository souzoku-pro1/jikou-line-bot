# DRAFT: LINE 応答品質改善（人間っぽさ）— 2相構成の改善設計

- TASK_ID: P2-BATCH-05 / TASK 14 起票 → **fix1（RLQ-H01/M01）で2相構成へ再編**
  ／記録日 2026-07-27
- 調査 BASE: origin/main `d87b3d6`（読取のみ）。**顧客データ・実ログ（App28 チャットログ・
  Railway ログ・承認キュー実レコード）には一切接触していない**。本書の引用はコード・
  プロンプト定数の構造のみ。
- 位置づけ: 応答方針 v2〜v2.4（トリアージ実測 118/120）は「**安全に正しく答える**」を
  完成させた。本トラックは次段「**人間らしく寄り添って答える**」の品質改善。
- **本書は設計 DRAFT であり実装しない**。
- **2相構成（RLQ-H01）**: 相1=**ログ非依存・先行可**（合成会話例のみで回す）／
  相2=**ログ依存・[人]ゲート後**（匿名化ログは別票 LINE-LOG-1・§4.1）。
  相2 の各工程の[人]承認ゲートは §4.4 に固定（RLQ-M01）。

## 1. 現行応答の生成経路（構造調査・実物参照）

### 1.1 生成パイプライン（chat_responder.py）

```
LINE webhook（main.py）
  → 対応モード判定（main.py:422-458: 「人対応」なら完全無言 early-return・
    App28 記録＋業務チャネル通知のみ・RV-10 S1 で redact）
  → handle_customer_message（chat_responder.py:1102-）
     ├ build_system_prompt（App21 の顧客情報を埋め込み・:585-）
     ├ 直近10往復の履歴 ＋ 今回メッセージ
     ├ _call_compose_reply（tool use・_COMPOSE_REPLY_TOOL :491-）
     │   出力 = reply / category(14種) / auto_send / jikou_update_flag /
     │          immediate_notice(6種) / reason（弁護士向け判断理由）
     ├ apply_server_guards（:672-: カテゴリ許可リスト・禁止語・必須文言・
     │   留保文言・テンプレ重複の二重チェック → 不合格は承認制へ降格）
     └ 分岐: auto_send → 即時返信／それ以外 → 承認キュー起票＋定型 ack
       （裁判所書類・危機対応等は専用即時文面 IMMEDIATE_NOTICE_TEXTS）
```

### 1.2 応答方針（プロンプト構造・_SYSTEM_PROMPT_BASE :272-）

「会話スタイル（最重要）」節に**既に人間っぽさの指示が存在する**（逐語）:

> - 自然な会話を最優先。テンプレートの棒読み感を出さない
> - 同じことを二度言わない。…既に確認済みの質問・確認事項は絶対に繰り返さない
> - 1メッセージは短く。…詳細は聞かれたら答える
> - 顧客への質問は一度に1つ、答えやすい形で聞く
> - 敬体（です・ます調）、1メッセージ400字以内を目安とする

### 1.3 品質に影響する構造上の特徴（観察）

- 定型文面が多層にある: 即時定型（PENDING_REPLY・CRISIS_SUPPORT_REPLY 等 :66-106）・
  FAQ定型・必須文言（FEE_REQUIRED_PHRASES・留保文言 B型 :192-）。**定型と生成文の
  接続部が「棒読み感」の主要リスク**（必須文言の挿入が文脈から浮く構造）。
- サーバーガードは**降格のみ**（品質を上げる方向の介入はない）。降格時の顧客体験は
  PENDING_REPLY（確認中）固定＝**待たされ感**の発生点。
- 履歴は直近10往復固定。長い相談での「前に言ったことを忘れる」体験の構造的上限。

## 2. 2相構成の全体像（RLQ-H01）

| 相 | 前提 | できること | ゲート |
|---|---|---|---|
| **相1**（§3） | **ログ不要**・現行コードとプロンプト構造のみ | 接続部・降格時文言・共感先出しの改善設計＋**合成会話例**での事前評価 | 文言変更 diff の弁護士承認（§3.3） |
| **相2**（§4） | **匿名化ログ**（別票 LINE-LOG-1・[人]実行）到着後 | 実会話由来の失敗パターン分類→改訂→検証 | §4.4 の工程別[人]ゲート |

- 相1 は今すぐ着手可能で、相2 の評価器（judge・軸定義）を**合成データで先に校正**
  しておく＝相2 のログが届いた時点で即実測に入れる構え。
  LINE-LOG-1 の**準備フェーズ（変換スクリプト・チェックリスト実装・合成データ試験）も
  相1 並行可**（fix2 M02・§4.1。実ログに触れるのは実行フェーズ＝G1/G2 後のみ）。

### 2.1 変更対象外（安全判定・送信経路）と STOP 関所 G0（fix2 H01・相1/相2 共通）

本トラックの**変更対象外**（v2〜v2.4 で確定した安全側の判定・経路。文言品質の改善で
これらに触れることはない）:
- **auto_send 判定**（`AUTO_SEND_CATEGORIES` の集合・compose_reply の auto_send 出力の扱い）
- **カテゴリ判定**（14 カテゴリの定義・自動送信可/承認必須の割当）
- **server guard**（`apply_server_guards`: 禁止語・必須文言・留保文言・テンプレ重複検査）
- **承認降格の仕組み**（降格条件・承認キュー投入経路）
- **immediate_notice**（危機対応・裁判所書類等の即時定型の選択肢と発火条件）
- **送信経路**（reply/push・チャットログ保存・承認キュー起票・弁護士通知の配線）
- **人対応モード判定・無言 early-return**（fix3 H01: `response_mode=人対応` の完全無言・
  App28 記録＋業務通知のみの経路。文言改善の対象にしない＝無言は無言のまま）
- **jikou_update_flag の意味論・更新経路**（fix3 H01: 時効更新事由の疑い判定の定義と
  その下流での扱い。プロンプト改訂がこの判定基準に触れることを禁じる）

（以上 **8 項目の閉集合**。追加・削除は本 DRAFT の改定＝Codex レビュー経由でのみ行う）

**STOP 関所 G0**: 失敗パターン分類・評価の結果が「上記の変更が必要」と示した場合、
本トラック内では**扱わず STOP** → **別票を起票し、独立安全レビュー（Codex・品質改善
トラックと利害分離）＋弁護士裁定**を経る。G0 は §4.4 の関所一覧に収載し、
**相1・相2 の双方に適用**する（相1 の文言改善が事実上のガード迂回になっていないかも
G0 の検査対象）。

## 3. 相1: ログ非依存の改善設計（先行可）

### 3.1 現行プロンプト構造の改善設計（対象3点）

1. **必須文言の接続部**: 費用注意書き・留保文言（B型）等の必須ブロックが本文と
   無接続に並置される構造を、「直前の顧客発話を受ける一文 → 必須文言 → 次の行動の
   一文」の**三段接続の型**に改める（必須文言そのものは一字も変えない＝v2.4 の
   安全確定を維持。変えるのは前後の書き方の指示のみ）。
2. **降格時文言の再設計案**: 承認制降格時の ack が PENDING_REPLY 一種で固定される
   構造を、**カテゴリ群別の ack 3〜4 種**（法的判断系=「先生に確認して◯◯までに
   お返事します」型／費用交渉系／クレーム系）へ多様化する案。期限を述べる場合の
   運用整合（実際の承認所要時間）は[人]確認事項。
3. **共感の先出し**: 不安・焦りの表明を検知した場合「受け止めの一文を手続き説明の
   前に置く」ことをプロンプトの会話スタイル節へ明示追加する案（現行は「自然な会話
   最優先」の一般指示のみで個別規定なし）。

### 3.2 チャッピー型評価の適用（合成会話例での事前評価）

- **合成会話例**: PC-A が典型シナリオから合成したスレッド 20〜30 本
  （不安表明/費用質問/督促パニック/雑談/長期無応答後の再開等・**顧客データ由来の
  情報を一切含まない**・repo 管理可能でレビュー可能）。
- 評価軸（7本・スレッド単位 1-5 採点）:

| 軸 | 定義 | 測り方 |
|---|---|---|
| 共感の先出し | 不安の表明に、手続き説明の**前に**受け止めの一文があるか | 判定型（LLM judge＋人抜き取り） |
| 文長の適正 | 長さが問いの重さに釣り合うか | 実測＋判定型 |
| 質問数の規律 | 一度に1問・答えやすい形か | 機械計数＋判定型 |
| 専門用語密度 | 法律用語の裸出現頻度 | 用語リスト機械計数 |
| 不安への応答速度 | 不安表明→降格 ack 固定文の率 | 機械計数 |
| 反復回避 | 既確認事項の再質問・同一定型再送 | 判定型＋機械検出 |
| 棒読み感 | 必須文言・定型の接続が浮いていないか | 判定型 |

- **judge の校正を相1 で完了させる**: 別系統 LLM を審査員とし、軸定義＋1-5 の
  係留例を与える。合成会話に対する judge 採点と[人]（弁護士）採点の一致率を測り、
  係留例を調整してから相2 に入る（審査員のモデル選定・費用は[人]確認）。
- 相1 の検証形: 現行プロンプト vs §3.1 改訂案で**合成スレッドへの応答を再生成→
  盲検比較**（顧客影響ゼロ・オフラインのみ）。

### 3.3 相1 の顧客影響ゲート

- §3.1 の改善はいずれも**顧客に届く文言に影響**するため、実装票では
  「変更 diff＋変更理由＋想定影響カテゴリ」の3点セットで**弁護士承認必須**。
  承認後もトリアージ回帰（120 ケース・TRIAGE_SCOPE=diff）を通してからデプロイ
  （マージ＝自動デプロイのためタイミングは[人]指示）。
- 必須文言（費用・留保・禁止語）の削除・緩和は本トラックでは提案しない。

## 4. 相2: ログ依存の改善ループ（[人]ゲート後）

### 4.1 LINE-LOG-1（匿名化ログ取得・**別票**・ここではスコープ定義のみ）

**フェーズ分離（fix2 M02）**:
- **準備フェーズ＝相1 並行可**: 変換スクリプト・残存検査・完全性チェックリストの実装と
  **合成データでの試験**（実ログ非接触のためログ依存なし）。Codex レビューもここで通す。
- **実行フェーズ＝相2（G1/G2 後のみ）**: raw export・変換実行・匿名化成果物の引渡し。

**必須要件（fix2 H02 で拡充）**:
- **実行者**: [人]（大野）。PC-A は生データに接触しない。
- **redaction（RV-10 禁止カテゴリの完全写像）**: 以下を**明示的に変換/除去対象**とする —
  (i) **自由記述 PII**（氏名・住所・電話・口座・生年月日・勤務先・家族構成等の
  本文中の言及） (ii) **token・credential 類**（URL query 内 token・API key 様文字列）
  (iii) **文書本文の引用**（戸籍・契約書・Q&A/FAQ 原文・**FAX 本文**〔fix3 H02〕等の
  長文引用は要旨化または除去）
  (iv) **vendor raw 情報**（API 生応答・エラー本文・スタックトレース）。
  変換は**許可フィールドの allowlist 方式**（許可した構造化項目以外は落とす）を正とし、
  禁止列挙は残存検査の二重化として使う。
- **相談本文のコーパス固有契約（fix3 H02）**: 評価に**必要最小限の発話のみ保持**する。
  長文の身の上話・債務経緯等の固有事実は**要旨化**（例:「複数社からの借入と滞納の経緯を
  説明」）し、**原文全文の収載は禁止**（引用は評価判定に必要な短い断片まで）。
- **allowlist の固定仕様（fix3 H02: フィールド名＋内容種別＋最大長＋変換規則）**:

| フィールド | 内容種別 | 最大長 | 変換規則 |
|---|---|---|---|
| thread_id | 連番仮名（C001…） | 8 | user_id→連番写像（対応表は raw 側にのみ保持・成果物へ含めない） |
| turn.role | enum（user/assistant） | 9 | 実値そのまま（enum 外は変換失敗＝fail-closed） |
| turn.text | 発話（相談本文契約適用済み） | 400 字 | redaction 写像＋要旨化＋残存検査通過のもののみ |
| turn.delivery | enum（auto/approved/demoted/immediate/silent） | 10 | 送信区分の写像（層別判定に使用） |
| turn.category | 既存 14 カテゴリ enum | 20 | 実値そのまま（enum 外は除外） |
| meta.amount_band | 金額の丸め帯（例: 40万円台） | 16 | 丸め規則（粒度は[人]裁定） |
| meta.date_band | 時期の丸め（例: 2015年頃） | 16 | 同上 |
| layer | 固定 enum 6値のみ（main:auto／main:approved／main:demoted／rare:silent／rare:immediate／rare:fallback） | 14 | 変換器の層割当（機械: 希少 a>b→主要3群排他）＋fallback 補正（reverify 時の[人]構造化入力・優先順位 a>b>c を再適用）で確定 |

  （改定 2026-07-27・LINE-LOG-PREP fix1 H02(i): 標本層ラベル `layer` を allowlist へ
  正式追加。本改定は上表 1 行と本注記の追加のみ）
  上表に**無いフィールドは出力しない**（allowlist の追加は本 DRAFT 改定＝Codex
  レビュー経由のみ）。
- **自由記述の残存検査**: 変換後コーパスに対し (a) パターン検査（電話/口座/郵便番号/
  連続数字/token 様） (b) [人]目視のチェックリスト（スレッドごとに残存 PII 無しを確認・
  署名）の二段を必須とする。
- **変換失敗時 fail-closed**: 変換に失敗した・判定不能のスレッドは**出力しない**
  （部分出力・「後で直す」持ち出しも禁止。失敗件数のみ記録して除外）。
- **保管・取扱い仕様（raw export と匿名化成果物を分離して規定）**:

| 項目 | raw export（変換前） | 匿名化成果物 |
|---|---|---|
| 保管場所 | [人]端末のローカルのみ（クラウド同期フォルダ禁止） | 司令塔管理領域（repo 外・コミット禁止） |
| アクセス者 | 大野のみ | 大野＋PC-A（レビュー時 Codex は成果物のみ） |
| 暗号化 | 端末のディスク暗号化必須＋アーカイブ化する場合はパスワード付き | 保管領域の暗号化（司令塔指定） |
| 転送方法 | **転送しない**（生成した端末上で変換まで完結） | 司令塔指定の経路のみ（メール添付平文は禁止） |
| 保持期限 | **export 時点から絶対上限7日（fix3 H02: 変換の成否にかかわらず適用**。期限内に変換未完なら raw を削除し再 export からやり直す） | 本トラック完了まで（完了後削除） |
| 削除証跡 | 削除日時を work-log に記録（[人]） | 同左 |

- **標本設計（fix2 M01→fix3 M01 で階層規則を固定）**:
  - **スレッド境界の定義（fix3・再現可能化）**: 同一 user_id の発話列を
    **最終発話から 72 時間以上の無応答**で分割した単位を 1 スレッドとする
    （分割規則は固定・機械適用可能）。「同一 user 最新1本」は、この境界で分割した
    後の**最新スレッド 1 本**を指す。
  - **割当順序（fix3・固定）**: ①**希少安全3層へ優先割当**（下記 (a)〜(c) の定義に
    合致するスレッドを先に確保）→ ②**残りを主要3群の排他定義で割当**
    （優先順位: 降格群＞承認送信群＞自動送信群・全数被覆）:
    降格群＝サーバーガード降格が1回以上／承認送信群＝降格なしで承認キュー経由送信
    （auto_send=false）が1回以上／自動送信群＝全送信が自動送信。
  - **希少安全3層（最低件数付き・fix3: 主要 30〜50 本の外数=別枠）**:
    (a) 人対応モード無言スレッド (b) 危機/裁判所向け即時通知
    （crisis_support／urgent_seizure_panic／court_doc_request）発火
    (c) 送信失敗 fallback（Claude outage・PENDING_REPLY 縮退）。**各層最低3件**。
    期間内の実例が3件に満たない層は**合成ケースで必須評価**（相1 の合成スレッド群に
    該当シナリオを必ず含めることで充足）。**各層の実例数と合成代替数は別集計**して
    記録する（例: 危機層＝実例2＋合成1）。
  - **抽出期間**: 直近90日。**重複排除**: 同一 user_id は最新スレッド1本のみ
    （境界定義は上記）。**各層最低数**: 主要3群 各10本（主要合計30〜50 本・件数調整は
    [人]）＋希少3層 各3本（**外数**・不足は合成で代替し別集計）。
- **Codex レビュー必須**: 抽出手順書・redaction 変換仕様・残存検査・完全性チェック
  リストは**実行前に** Codex レビューを通す（PII 漏れの第三者確認・準備フェーズ内）。

### 4.2 失敗パターン分類

匿名化コーパスに §3.2 の評価（judge は相1 校正済み）を掛け、失敗を分類:
説明過多／共感欠落／定型接続の不自然／既出の再質問／専門用語の裸出し／
降格過剰による待たされ（ガード側課題と文言課題を分離）。分類結果から
§3.1 の改訂第2弾（実会話由来）を起票する。
**ガード側課題（変更対象外・§2.1）に分類されたものは G0 の STOP 対象**であり、
本トラックの改訂候補には入れない（別票＋独立安全レビュー＋弁護士裁定へ）。

### 4.3 A/B 検証

- **段階1（オフライン・顧客影響ゼロ）**: 改訂前後で匿名化スレッドへの応答を
  再生成し盲検比較。**初版はこの段階のみ**。
- **段階2（本番）**: ユーザー分割 A/B は法律事務所の応対として許容できるかの
  **[人]判断が先**。既定は「段階1で優位確認できた改訂のみ全量切替・期間比較で代替」。

### 4.4 工程別[人]承認ゲート（RLQ-M01・関所一覧。G0/G4 は相1/相2 共通）

| # | 工程 | ゲート内容 | 承認者 |
|---|---|---|---|
| **G0** | **安全判定・送信経路への波及（STOP 関所・§2.1）** | 失敗分類/評価が変更対象外（**§2.1 の8項目**）の変更を要すると示した場合は**本トラック STOP→別票＋独立安全レビュー＋弁護士裁定**。**相1/相2 共通**（R-LINE-Q-4 L01: 列挙は §2.1 の閉集合を単一の正とし本表で再列挙しない） | **弁護士裁定必須** |
| G1 | LINE-LOG-1 の実行 | 抽出手順・redaction 仕様・残存検査の Codex レビュー通過（準備フェーズ完了）＋実行判断 | [人]（実行も[人]） |
| G2 | コーパスの PC-A への引き渡し | 残存 PII の目視チェックリスト完了 | [人] |
| G3 | 失敗パターン分類結果→改訂案の採否 | 分類レポートの確認・改訂対象の選定 | [人] |
| G4 | **文言変更（顧客影響関所）** | 変更 diff＋理由＋影響カテゴリの3点セット承認（§3.3 と同一関所・相1/相2 共通） | **弁護士承認必須** |
| G5 | デプロイ | トリアージ回帰通過確認＋マージタイミング指示 | [人] |
| G6 | 段階2（本番 A/B）への移行 | 応対の一貫性の許容判断 | [人]（既定=実施しない） |

## 5. 実装票スコープ案と優先順位

| 優先 | 票案 | 相 | スコープ | ゲート |
|---|---|---|---|---|
| 1 | LINE-Q-001: 評価ハーネス＋合成会話例 | 相1 | §3.2（軸定義・judge プロンプト・係留例・合成スレッド 20〜30 本・機械計数） | judge モデル選定=[人] |
| 2 | LINE-Q-002: 相1 文言改善第1弾 | 相1 | §3.1 の3点の diff 案＋合成スレッド盲検比較＋トリアージ回帰 | **G0**/G4/G5 |
| 3 | LINE-LOG-1: 匿名化ログ取得 | **準備=相1 並行可／実行=相2 入口**（fix2 M02） | §4.1（準備: 変換スクリプト・残存検査・チェックリスト実装＋合成データ試験＋Codex レビュー／実行: raw export→変換→引渡し） | 準備は Codex レビュー・実行は G1/G2 |
| 4 | LINE-Q-003: 初回実測と失敗パターン報告 | 相2 | §4.2（コーパス評価→分類→改訂候補提案書） | G3 |
| 5 | LINE-Q-004: 応答方針 v3 改訂（実会話由来） | 相2 | 上位失敗パターン 2-3 件の diff＋段階1検証＋回帰 | **G0**/G4/G5 |
| — | （保留） | 相2 | 段階2 本番 A/B・履歴10往復拡張 | G6 |
- 相1（LINE-Q-001/002）は**ログ・[人]ゲートの完了を待たず今すぐ起票可能**。
  相2 は LINE-LOG-1 の G1/G2 通過が入口。
- **全実装票共通の完了条件（fix3 H01）**: 完了報告に**「§2.1 変更対象外（8項目）への
  変更ゼロ」の確認**（対象ファイルの diff で機械確認・確認方法と結果を明記）を含める。
  変更が必要になった場合は完了ではなく **G0 STOP**（§2.1）。
