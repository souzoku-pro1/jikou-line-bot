# App 38（予定）「仕分けログ」作成 完全指示書（Computer Use 用）

**開いている kintone でアプリ「仕分けログ」を新規作成する。以下の手順を上から順に実行する。**

この指示書は自己完結している。外部の設計書・リポジトリを参照する必要はない。
フィールド数は **13個**（書類仕分けシステム第1段の判定ログ・照会/確定の台帳。
レコード番号フィールドは kintone が自動で持つため追加しない）。
アプリ番号は作成順で自動採番される（38 は予定。実番号は完了報告の URL で確定する）。

---

## 0. 前提確認（最初に必ず行う）

1. 現在のブラウザタブに **kintone のポータル画面**（URL が `https://～.cybozu.com/k/` で始まり、「アプリ」の一覧が見える画面）が開いていることを確認する。
2. **開いていない場合・ログイン画面が出ている場合・別のサービスが表示されている場合は、一切の操作をせず作業を中止し、「kintone ポータルが開いていないため中止した」と報告して終了する。**
3. 途中で予期しないダイアログ・エラー・確認画面が出た場合も、無理に進めず、その画面の内容を報告して停止する。

---

## 1. アプリの新規作成

1. **＋ ボタン（アプリを作成）** → **「はじめから作成」**
2. アプリ名欄を次の名前に書き換える：

```
仕分けログ
```

---

## 2. フィールド追加の共通操作（この2つの手順を各フィールドで繰り返す）

**手順A:** パーツをドラッグ＆ドロップ → 歯車 → 「設定」 → フィールド名を**コピペ**
**手順B:** 設定画面下部の**フィールドコード**を確認し、指示と違えば**コピペで**修正 → 保存

⚠ フィールド名・フィールドコード・選択肢は**必ずコピペ**。**選択肢の末尾に空白を入れない。**

---

## 3. フィールドの追加（13個・この順番で）

### フィールド 1/13
- パーツ: **文字列（1行）**
- フィールド名: `ファイル名`／フィールドコード: `ファイル名`

### フィールド 2/13
- パーツ: **文字列（1行）**
- フィールド名: `Drive_fileId`／フィールドコード: `Drive_fileId`
- ⚠ 英字の大小をこの表記のとおりに（`D` は**半角大文字**・`file` は小文字・末尾の `Id` は **半角大文字の I**＋小文字の d）

### フィールド 3/13
- パーツ: **文字列（1行）**
- フィールド名: `Drive_URL`／フィールドコード: `Drive_URL`
- ⚠ `URL` は**すべて半角大文字**

### フィールド 4/13
- パーツ: **文字列（1行）**
- フィールド名: `書類種類`／フィールドコード: `書類種類`

### フィールド 5/13
- パーツ: **数値**
- フィールド名: `確信度`／フィールドコード: `確信度`

### フィールド 6/13
- パーツ: **文字列（複数行）**
- フィールド名: `判定理由`／フィールドコード: `判定理由`

### フィールド 7/13
- パーツ: **文字列（複数行）**
- フィールド名: `候補一覧`／フィールドコード: `候補一覧`

### フィールド 8/13
- パーツ: **文字列（1行）**
- フィールド名: `仕分け先レコードID`／フィールドコード: `仕分け先レコードID`
- ⚠ `ID` は**半角大文字**

### フィールド 9/13
- パーツ: **文字列（1行）**
- フィールド名: `仕分け先氏名`／フィールドコード: `仕分け先氏名`

### フィールド 10/13
- パーツ: **文字列（1行）**
- フィールド名: `仕分け先フォルダ名`／フィールドコード: `仕分け先フォルダ名`

### フィールド 11/13
- パーツ: **ドロップダウン**
- フィールド名: `状態`／フィールドコード: `状態`
- 選択肢（4つ・上から順に）:
```
照会中
確定
実行済み
取消
```
- 初期値: `照会中` を選択

### フィールド 12/13
- パーツ: **日時**
- フィールド名: `確定日時`／フィールドコード: `確定日時`
- 初期値: **「レコード登録時の日時を初期値にする」のチェックを外す**

### フィールド 13/13
- パーツ: **日時**
- フィールド名: `実行日時`／フィールドコード: `実行日時`
- 初期値: **「レコード登録時の日時を初期値にする」のチェックを外す**

---

## 4. 保存前のセルフチェック（必ず実施）

1. **13個すべて**のフィールドコードを 歯車 → 設定 で §3 と一字一句突合する
2. 特に次の4点を重点確認:
   - `Drive_fileId` / `Drive_URL` の英字大小がこの表記のとおり
   - `仕分け先レコードID` の `ID` が**半角大文字**
   - `状態` の選択肢が `照会中` / `確定` / `実行済み` / `取消`（この順）・初期値 `照会中`
   - `確定日時` / `実行日時` の**初期値チェックが外れている**こと
3. フィールドの総数が **13個**であること

---

## 5. 保存と公開

1. **「フォームを保存」** → **「アプリを公開」**
2. 完了表示を確認する

---

## 6. 完了報告（この様式で報告する）

```
アプリURL: https://～.cybozu.com/k/NN/
フィールドコード一覧:
1. ファイル名
（…13まで）
相違・未確認: （あれば列挙、なければ「なし」）
```

---

## 7. 禁止事項

- 指示書に**ないフィールドの追加**（ラベル・スペース・罫線・テーブルも置かない）、指示された13個以外の**削除**
- フィールド名・フィールドコード・選択肢の**言い換え・追加・並べ替え**（一字一句この指示書のとおり）
- アクセス権・APIトークン・Webhook・一覧・通知の設定（**今回のスコープ外**。別途人間が実施する）
- 他の既存アプリを開く・変更する・削除すること
- 迷ったとき・画面が指示と食い違うときに推測で操作を続けること（**停止して報告**する）
