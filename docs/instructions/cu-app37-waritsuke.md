# App 37（予定）「割付」作成 完全指示書（Computer Use 用）

**開いている kintone でアプリ「割付」を新規作成する。以下の手順を上から順に実行する。**

この指示書は自己完結している。外部の設計書・リポジトリを参照する必要はない。
フィールド数は **11個**（正本: docs/souzoku-shorui/01-data-schema.md §3〔App 38 割付〕からの転記。
レコード番号フィールドは kintone が自動で持つため追加しない）。
アプリ番号は作成順で自動採番される（37 は予定。実番号は完了報告の URL で確定する）。

---

## 0. 前提確認（最初に必ず行う）

1. 現在のブラウザタブに **kintone のポータル画面**（URL が `https://～.cybozu.com/k/` で始まり、「アプリ」の一覧が見える画面）が開いていることを確認する。
2. **開いていない場合・ログイン画面が出ている場合・別のサービスが表示されている場合は、一切の操作をせず作業を中止し、「kintone ポータルが開いていないため中止した」と報告して終了する。**
3. 途中で予期しないダイアログ・エラー・確認画面が出た場合も、無理に進めず、その画面の内容を報告して停止する。

---

## 1. アプリの新規作成

1. **＋ ボタン（アプリを作成）** → **「はじめから作成」**
2. アプリ名欄を次の名前に書き換える：

```
割付
```

---

## 2. フィールド追加の共通操作（この2つの手順を各フィールドで繰り返す）

**手順A:** パーツをドラッグ＆ドロップ → 歯車 → 「設定」 → フィールド名を**コピペ**
**手順B:** 設定画面下部の**フィールドコード**を確認し、指示と違えば**コピペで**修正 → 保存

⚠ フィールド名・フィールドコード・選択肢は**必ずコピペ**。**選択肢の末尾に空白を入れない。**

---

## 3. フィールドの追加（11個・この順番で）

### フィールド 1/11
- パーツ: **ドロップダウン**
- フィールド名: `ユニット種別`／フィールドコード: `ユニット種別`
- 選択肢（4つ・上から順に）:
```
時効援用
相続放棄
相続一般
補助金
```
- 初期値: 設定しない

### フィールド 2/11
- パーツ: **文字列（1行）**
- フィールド名: `案件アプリID`／フィールドコード: `案件アプリID`

### フィールド 3/11
- パーツ: **文字列（1行）**
- フィールド名: `案件レコードID`／フィールドコード: `案件レコードID`

### フィールド 4/11
- パーツ: **文字列（1行）**
- フィールド名: `被相続人名表示用`／フィールドコード: `被相続人名表示用`

### フィールド 5/11
- パーツ: **文字列（1行）**
- フィールド名: `財産レコードID`／フィールドコード: `財産レコードID`

### フィールド 6/11
- パーツ: **文字列（1行）**
- フィールド名: `相続人レコードID`／フィールドコード: `相続人レコードID`

### フィールド 7/11
- パーツ: **ドロップダウン**
- フィールド名: `取得区分`／フィールドコード: `取得区分`
- 選択肢（6つ・この順で。括弧は**全角**）:
```
単独取得
共有取得
換価分割
代償取得
債務引受
保険金受取（みなし）
```
- 初期値: 設定しない

### フィールド 8/11
- パーツ: **文字列（1行）**
- フィールド名: `持分`／フィールドコード: `持分`

### フィールド 9/11
- パーツ: **数値**
- フィールド名: `代償金額`／フィールドコード: `代償金額`

### フィールド 10/11
- パーツ: **文字列（複数行）**
- フィールド名: `条件メモ`／フィールドコード: `条件メモ`

### フィールド 11/11
- パーツ: **ラジオボタン**
- フィールド名: `有効`／フィールドコード: `有効`
- 選択肢（2つ・半角小文字・**この順**）:
```
yes
no
```
- 初期値: `yes` を選択

---

## 4. 保存前のセルフチェック（必ず実施）

1. **11個すべて**のフィールドコードを 歯車 → 設定 で §3 と一字一句突合する
2. 特に次の3点を重点確認:
   - `財産レコードID` / `相続人レコードID`（ID は**半角大文字**）
   - 取得区分の `保険金受取（みなし）` の括弧が**全角**
   - `有効` の選択肢が `yes` / `no`（この順）・初期値 `yes`
3. フィールドの総数が **11個**であること

---

## 5. 保存と公開

1. **「フォームを保存」** → **「アプリを公開」**
2. 完了表示を確認する

---

## 6. 完了報告（この様式で報告する）

```
アプリURL: https://～.cybozu.com/k/NN/
フィールドコード一覧:
1. ユニット種別
（…11まで）
相違・未確認: （あれば列挙、なければ「なし」）
```

---

## 7. 禁止事項

- 指示書に**ないフィールドの追加**（ラベル・スペース・罫線・テーブルも置かない）、指示された11個以外の**削除**
- フィールド名・フィールドコード・選択肢の**言い換え・追加・並べ替え**（一字一句この指示書のとおり）
- アクセス権・APIトークン・Webhook・一覧・通知の設定（**今回のスコープ外**。別途人間が実施する）
- 他の既存アプリを開く・変更する・削除すること
- 迷ったとき・画面が指示と食い違うときに推測で操作を続けること（**停止して報告**する）
