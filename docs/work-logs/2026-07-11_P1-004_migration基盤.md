# 作業記録 2026-07-11: P1-004系 — migration基盤（alembic + hub/db.py）クローズ

- Phase/Gate: Phase 1（基盤hardening）・P1-003〜P1-004e
- 記録日: 2026-07-11 ／ 実施: Claude Code（PC-A）／ 出典: 各COMPLETION_REPORT・
  Codexレビュー・git/PR/Railway実出力（推測補完なし）

## 0. 本番読取確認（本記録作成時点）

`GET https://jikou-line-bot-production.up.railway.app/health` → **HTTP 200**
`{"status":"ok","deps":{"python-docx":"ok","reportlab":"ok (ipaexg.ttf 同梱)","graphviz":"ok (/usr/bin/dot)"}}`

## 1. P1-003系の経緯（SHA齟齬の解消とDB前提の成立）

- 引き継ぎ記録の齟齬: 指示のBASE_SHA `9de5060` は**repoに不実在**（PR #99未マージが原因）
  → 実測 `87a4938` で停止報告（P1-003）→ P1-003aでPR #99マージ → main `3ef395d` で再固定
- `DATABASE_URL` 注入（人操作・Railway Variables参照 `${{Postgres.DATABASE_URL}}`）
  → P1-003cで可視性 True を確認（値は終始不読・boolean確認のみ）
- **DATABASE_PUBLIC_URL 追加の経緯**: `railway run` はローカル実行＋env注入のため、
  内部ホスト `postgres.railway.internal` はPC-Aから**構造的に到達不能**
  （getaddrinfo failed・接続前失敗で副作用ゼロ）。PC-Aからのmigration実行用に
  `DATABASE_PUBLIC_URL = ${{Postgres.DATABASE_PUBLIC_URL}}` を人操作で追加（司令塔裁定・案A）

## 2. P1-004/004a/004c: 設計判断とレビュー3巡

設計判断（確定）:
- D1: 追加依存のみ==固定（sqlalchemy==2.0.51 / alembic==1.18.5 / psycopg[binary]==3.3.4）
- D2: migrationは明示コマンドのみ（起動時自動upgrade禁止・AST機械強制）
- D3: DATABASE_URL不在でも正常起動（lazy初期化・未設定到達時のみ明示エラー）
- D4: エンジン/セッション生成は hub/db.py 一点集約（alembic/env.py も get_engine() 経由）
- D5: 初回migrationは空baseline 1本のみ
- **D6（P1-004cで確定）: disposeの責任分離** — `adispose_all()`（async・正規cleanup API・
  await AsyncEngine.dispose）／同期 `dispose_all()` はループ外=asyncio.runで正しく閉じ、
  ループ内=明示例外。「sync_engine.dispose()で足りる」前提は撤回

Codexレビュー3巡:
| rev | SHA | 判定 → 対応 |
|---|---|---|
| R-P1-004 | `f05e695` | CHANGES_REQUIRED → 004aで H01（env.py独自create_engine廃止）/M01（動的alembic起動検出）/M03（credential非漏洩テスト）/L01 対応 |
| R-P1-004a | `1f02984` | CHANGES_REQUIRED → 004cで L01正規解消（D6）＋dispose実効性spy＋URL文字列化AST禁止（M02一部） |
| R-P1-004c | `5ecd1f5` | **PASS_WITH_FINDINGS（デプロイ可）** → PR #100マージ（main `fd787fe`） |

## 3. P1-004d: baseline本番適用（司令塔がPC-A実行を明示許可）

- `DATABASE_PUBLIC_URL` 存在=True確認 → プロセス内でDATABASE_URLへ差し替えて
  `alembic upgrade head` 実行（値は一切非表示）
- **`alembic current` = `e0b2858b3ab0 (head)`**
- publicスキーマの全テーブル実測 = **`['alembic_version']` のみ**（D5どおり・業務影響ゼロ）

## 4. テスト推移

**1,072 → 1,088（004）→ 1,097（004a）→ 1,102（004c）**・全区間 FAIL 0・
既存テストの削除/skip/緩和なし（subtests 231）。

## 5. DEFER台帳の更新

追記（本記録で登録）:
- **RCF-M03改め: offline URL分離**（旧M04）— alembic offlineモード用URLと実行URLの分離設計
- **RCF-L03: async dispose例外時のbest-effort化**（RP1004C-L01）— 現仕様は
  「finallyでキャッシュ必ずクリア＋例外送出」。複数エンジンの部分dispose失敗時の
  継続戦略はP1-005以降で裁定
既存（不変）: RCF-M01（再送idempotency）/RCF-M02（LINE非2xx検知）/
RCF-L01（IDログ台帳化）/RCF-L02（path secret HMAC化）
※P1-004系レビュー由来のDEFER: M02残り（import graph強化）・M05（lock/SBOM）も未消化のまま維持。

## 6. P1-005への申し送り

1. **shutdown hookは `await adispose_all()` 必須**（FastAPIのlifespan/shutdownに結線・contract test込み）
2. hub/db.py **自身**への直接URL出力禁止テスト（現ASTテストはhub/db.pyを除外している）
3. M01の除外リスト見直し（alembic/配下）・M02（import graph）・log capture検査
4. **実DB込みのdispose統合テスト**（P1-004cはspy検証まで。実DB使用時に追加）
5. README追記: PC-Aからのmigrationは `DATABASE_PUBLIC_URL` 経由
   （railway runはローカル実行＝internal host不達、の構造説明を含める）
6. InboundEvent / IngestionReceipt テーブルはmigration経由でのみ追加（手動DDL禁止）

## 7. 規律記録

- **指示文切断**: P1-004の指示はIN_SCOPE途中で切断されて届いた。PC-AはD1〜D5から
  補完実施し報告冒頭で明示（結果は受理）。**今後の規律: 切断を検知したら補完せず
  停止して再発行を求める**（司令塔裁定・本記録で固定）
- **本番変更コマンドの境界**: 平文でコマンドが届いた場合（P1-004d前の
  `railway run alembic upgrade head`）でも、本番DB変更はTASK_MODE=MIGRATION等の
  **票による明示許可を境界**とする再確認。※当該実行は内部ホスト不達により
  接続前失敗・副作用ゼロだった
- 値の扱い: DATABASE_URL/PUBLIC_URLの実値は全工程で不読・不表示
  （boolean存在確認とプロセス内差し替えのみ）
