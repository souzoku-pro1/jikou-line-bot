# 作業記録 2026-07-17: P1-POSTMERGE-136-138（PR #136/#138/#137 クローズ検分）

- TASK_ID: P1-POSTMERGE-136-138／実施: PC-A（READ_ONLY 検分＋docs 作成）
- 旧 main: 2b33ffa ／ **新 main: e7e7f2a**（FF 追従・直接 commit なし）

## 1. マージ 3 本（origin/main）

```
e7e7f2a Merge pull request #137 from souzoku-pro1/feat/rv04c-s2-gas-signing   （2026-07-17 15:06:57 JST）
80baf14 Merge pull request #138 from souzoku-pro1/feat/rv04c-s3-kintone-lane   （15:06:36 JST）
5acf37c Merge pull request #136 from souzoku-pro1/docs/small-batch-close       （15:06:17 JST）
```
- #136=小粒バッチ クローズ work-log（docs のみ）／#138=RV-04c S3 kintone レーン（+S3-fix1/fix2）／
  #137=RV-04c S2 GAS 署名（+S2-fix1/fix2）。

## 2. 全 suite 実測（新 main e7e7f2a・worktree）

```
$ PYTHONUTF8=1 python -m pytest -q
1 failed, 1458 passed, 5 warnings, 588 subtests passed in 87.09s
FAILED test_triage_classification.py::TestTriageClassification::test_classification_accuracy
```
- **1,458 passed / 1 failed（triage・既知）/ 0 skipped**。
- 基準値照合: 旧 main 2b33ffa=**1,379** ＋ `test_rv04c_kintone_lane`（46）＋
  `test_rv04c_gas_builder`（33）＝**1,458**（+79・厳密一致）。branch 単独値（S3=1,425/
  S2=1,412）は各 branch が自分の新テストのみ持つため。main は両 branch 分を合算。
- **テスト削除・skip 追加・期待値緩和なし**を確認: 総数が +79 で単調増加・`skipped` 0 件・
  新テスト 2 ファイルに `@skip`/`skipTest` マーカー 0 件。
- 唯一の FAIL は `@skipUnless(ANTHROPIC_API_KEY)` の実 Claude API テストが full suite の
  env 漏れで dummy キー実行され落ちる**既存アーティファクト**（base 2b33ffa でも同一・
  real key の `railway run` では pass）。**triage 以外の FAIL なし**。

## 3. デプロイ検分（READ_ONLY）

- **/health**: HTTP **200**・`{"status":"ok","deps":{python-docx:ok, reportlab:ok, graphviz:ok}}`。
- **起動ログ**（deployment `c0979767`・2026-07-17 06:07:24 UTC=15:07:24 JST 起動＝#137 マージ
  27 秒後の post-merge 世代）: scheduler 2 ジョブ登録（RETURN_DEADLINE/HEALTHCHECK）・
  `Application startup complete`・**traceback/エラーなし**。flag 系ログ（[RV04c]/[RV05]/kintone）
  は出力なし＝**全 flag 未設定=OFF**（本番挙動無変化）。
- **変化の兆候なし**（Online 継続・flag OFF）。

## 4. 成果物（本 work-log と同 PR）

- `docs/runbooks/2026-07_S4-S5_cutover-checklist.md`（新規）: GAS 実機テスト 3 関数・secret 生成/
  投入・M03 共同編集者ゼロ実見・点火順序（dual-accept→watcher 結線→署名実測→並行観測→
  legacy 段階停止→rotation 4工程+3小工程→retirement 3点）。各工程に [人]/[PC-A]/[司令塔]
  ラベルと停止条件。**`INBOUND_EVENT_DURABLE_ENABLED` 点火は含めない**（別裁定）。

## 5. 本番・外部効果

- **なし**（READ_ONLY 検分のみ・push/env/本番変更なし）。マージ＝auto-deploy で post-merge
  世代が起動済みだが flag 全 OFF で挙動無変化。
