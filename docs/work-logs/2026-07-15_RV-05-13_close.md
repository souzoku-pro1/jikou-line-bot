# 作業記録 2026-07-15: RV-05/13 クローズ（durable inbound 一式の本番反映）

- Phase/Gate: Phase 1・G1（process-memory critical queue 廃止）／G3（durable commit 前に成功 ACK を返さない）
- 正本: `docs/design-drafts/DRAFT_RV05_DURABLE_INBOUND.md`（rev5・fix5/6 で §H-01 に既知制約追記）
- 本記録は RV-05/13 の統合クローズ。詳細は各 work-log を参照（重複記載しない）。

## 1. 経緯サマリ

| 段階 | 内容 | 記録 |
|---|---|---|
| DRAFT | D〜D5（レビュー R-D〜R-D5・**5巡**）で設計確定（rev5） | `docs/design-drafts/DRAFT_RV05_DURABLE_INBOUND.md` |
| 実装 | ledger core（epoch fencing）・migration・LINE Phase A・sortation 同期台帳・flag 既定 OFF | `2026-07-14_RV-05-13_durable-inbound-impl.md` |
| fix1 | レビュー所見修正（review-findings） | `2026-07-14_RV-05-13-fix_review-findings.md` |
| fix2 | partial insert・lease | `2026-07-14_RV-05-13-fix2_partial-insert-and-lease.md` |
| fix3 | 排他 claim・lease 再定量 | `2026-07-14_RV-05-13-fix3_exclusive-claim-and-lease.md` |
| fix4 | stale processing 回収・ページ上限・attempts 加算統一 | `2026-07-14_RV-05-13-fix4_stale-processing-recovery.md` |
| fix5 | 初回 claimed_at（H-NEW-02）・既知制約明文化・人手 reset runbook・M-DOC-01 | `2026-07-15_RV-05-13-fix5_claimed-at-and-known-limits.md` |
| fix6 | runbook 安全主張の撤回・DRAFT §H-01 矛盾解消・attempts 条件（docs のみ） | 同上（fix6 改訂節） |
| レビュー | 実装後 R-RV-05-13〜R-RV-05-13-7 の**7巡**（Codex 第三者レビュー） | 各 fix work-log の所見対応表 |
| マージ | **PR #131 → main `b09bc70`**（2026-07-15 15:11:54 JST・マージ＝Railway auto-deploy） | — |
| migration | **`c4f1a2b7d8e9`**（ingestion_receipt＋processing_attempt・既存表 ALTER なし）適用。適用後 `alembic current` = `c4f1a2b7d8e9 (head)` 実測 | — |
| デプロイ検分 | READ_ONLY・flag 未投入で**全 PASS**: Online（deployment `d17a5b2e`・b09bc70 マージ 47 秒後起動）・起動ログ traceback/import エラー 0・/health 200・flag NOT SET・scheduler 2 ジョブ登録 | — |

## 2. 最終テスト

- **1,370 passed**（fix5 時点の全 suite・base a1d35b5 比 +1・回帰ゼロ）。fix6 は docs のみ（.py 非接触）。
- 既存 **1 FAIL** は `test_triage_classification`（実 Claude API テストが full suite の env 漏れで
  dummy キー実行され落ちる**既存アーティファクト**・base 同一・real key の `railway run` では pass）。

## 3. 逸脱記録

1. **branch ポインタ混乱 2件**（ローカル main の誤指し・feat ブランチの誤リセット→push 空振り→復元で解消）。
   **恒久策**: ローカル main は origin 追従専用（直接 commit・checkout 作業をしない）・branch 操作の
   直後に `git rev-parse` で期待 SHA を機械確認する。
2. **Codex 安全フィルタによる 1回のブロック**（レビュー依頼文の言い換えで解消・レビュー対象/内容の変更なし）。
3. **手順②-1 省略での migration 直接適用**（適用コマンドの実出力で前提 revision
   `b7d3e1a9c2f4 → c4f1a2b7d8e9` の遷移を確認済み・実害なし）。

## 4. 既知制約（Phase A・H-NEW-01-R3）

- LINE の stale processing 行は**再配送終了後、自動回収の契機を失い人手 reset 依存**
  （回収駆動が LINE 再配送のみのため。次イベントは別 dedup_key＝旧行を回収しない）。
- **検知** = §6 観測（最古 non-terminal 滞留時間・収束率低下・dead-man 統合）。
- stale 再 claim と生存旧 task の**まれな併走（二重返信）は比較裁定で受容**
  （検知可能な2回返信 > 検知困難な0回沈黙・fix4 work-log §3.3）。
- **runbook**: `2026-07-15_RV-05-13-fix5_claimed-at-and-known-limits.md` §4（fix6 改訂:
  推奨手順=デプロイ再起動→reset・attempts 判定つき UPDATE 完成形・実行前のログ生存目視）。
- 逸脱の解消は RV-06（durable replay 化・DRAFT §H-01）。

## 5. flag ON 前提の整理

- **`INBOUND_EVENT_DURABLE_ENABLED` の投入は K4 確認（LINE redelivery 設定・大野）後に裁定**。
  LINE 側は再配送が re-attempt／stale 回収の駆動源のため、K4 の設定実態（ON/OFF・挙動）を
  確認してから投入タイミングを決める（DRAFT §9.2 K4）。
- **sortation 側（ingestion_receipt 台帳）は K4 と独立に ON 可**（駆動源は GAS 再送・LINE 再配送に
  依存しない）。flag は両経路共通のため、切り分けが要る場合の運用は司令塔裁定。

## 6. 次段申し送り

- caller 移行（K1-K3）
- R4-2 soft merge
- App 36/37
- dependency lock
- 小粒: P1-113/114・RCF-M08

## 7. 枠消化の日次一行

- 2026-07-15: RV-05/13 クローズ work-log 固定（P1-DOC-RV0513・docs のみ・自己マージ）。
  開始/終了とも **モデル実測 = Fable 5（claude-fable-5）**。
