# 作業記録 2026-07-12: P1-104 S2応答body最小化+例外分類化（RV-10 PR-3）／P1-105B dead-man事前確認

- Phase/Gate: Phase 1・RV-10 PR-3（台帳消化）＋ P1-105B（READ_ONLY 事前構成確認）
- 実施: Claude Code（PC-A）／レビュー: Codex（R-P1-104・1巡 PASS）
- 結果: **PR #114 マージ済み**（base `93a74a2` → マージ後 main = `d4bc94cb8153edcd4c0e548509c0522f2de5f79e`）
- 全suite: **1,228 passed / FAIL 0**（`--ignore=test_triage_classification.py`・+299 subtests）／AST 3検査（no_new / stale / monotonic）PASS

## 1. task_id

- **P1-104**（RV-10 PR-3・S2応答body最小化+例外分類化）
- **P1-105B**（dead-man 明朝初回実行の事前構成確認・READ_ONLY）

## 2. P1-104 変更内容

- **chat_responder.py raw_error 5件**: logger の `resp.text`（kintone/vendor 応答本文＝§13.1 で通常 log に含めない項目）を `emit(vendor_raw)` で抑止、status は `emit(count)` で維持（App21検索 / chatlog保存 / chatlog取得 / 承認取得 / 承認送信済み更新）。
- **main.py stripe 未認証経路**: `/webhook/stripe` 署名検証失敗で `str(e)` を未認証 caller へ返す挙動を除去。内部詳細は `emit(vendor_raw)` 経由の構造化 log（既定=完全抑止）へ、応答は固定文言「署名の検証に失敗しました」＋400 のみ。
- **main.py httpexception f-string 14件**（/ocr/fixed-asset・/scan）: 例外分類化。生例外 `{e}`・抽出データ `{extracted}`・所在地/地番・env キー名・folder 入力 echo を body から除去し固定文言/error code 化。HTTP ステータス（4xx/5xx区分）は不変。
- **台帳（redaction_sink_allowlist.json）**: **207 → 188（-19）**。`baseline_count=211` 上限据え置き。新規 sink 違反ゼロ（新規コードは emit/定数のみ）。出力は `from hub.redact import emit`（1形式）経由・main.py へ module-level emit import と `logger` 新設。

## 3. テスト

- 全suite **1,228 passed / FAIL 0 / error 0**（基準 1,228 から減少なし・テスト追加なし）。
- AST 3検査 PASS。旧文言（生値埋め込み）を検証する既存テストは無く、期待値修正は不要（body 文言変更は「仕様変更」であり緩和ではない・ステータス意味は維持）。

## 4. Codex レビュー（R-P1-104）

- **1巡で PASS（finding 0）**。
- 不足テスト提案 4件は**司令塔裁定で DEFER → PR-5 へ編入**（本 PR は機械的台帳消化のため、テスト増強は別票で扱う）。

## 5. P1-105B 結果（dead-man 事前構成確認・READ_ONLY）

**PASS**。根拠:
- **発火時刻**: 毎朝 7:00 JST。`hub/scheduler.py:20`（`_JST=UTC+9`）＋ `:74-80`（全 JST-aware で次回計算・UTC 誤解釈なし）。既定 `HEALTHCHECK_HOUR_JST` 未設定＝7。起動配線 `main.py` startup → `start_healthcheck_scheduler()`・`HEALTHCHECK_DISABLED` 未設定＝有効。
- **送信先解決**: 死活通知は `notify_admin_line` → `push_line_message(token_env=business_token_env())` ＝ `DISPATCHBOT_CHANNEL_ACCESS_TOKEN`（存在=True）。宛先は `ATTORNEY_LINE_USER_ID`（存在=True・`LINE_ADMIN_USER_ID` 未設定のためフォールバック）で allowlist 通過。
- **テーブル**: `notify_heartbeat` 本番 DB に実在・**行数 0（空）**・`alembic current = 3e59f8270aa8 (head)`（migration #4 適用実在）。
- **fail-closed**: `business_token_env()` は常に DISPATCHBOT 定数を返し、`push_line_message` は token 欠落時に送信ゼロ＋警告で終了（顧客Bot `LINE_CHANNEL_ACCESS_TOKEN` へフォールバックしない）。

**司令塔裁定（P1-105B 付随）**:
- 初回 7:00 の「業務通知の成功記録が1件もありません（空）」警報は**想定内のブートストラップ**（その警報送信自体が最初の heartbeat を seed する）。実障害として扱わない。
- stale 閾値 **25h は維持**。低トラフィック時の約2日周期オシレーション懸念は **RCF-M08 として観察後に再裁定**（本タスクでは変更しない）。

## 6. 本番効果

- PR #114 マージで **Railway auto-deploy 発火**（schema/env 変更なし・適用作業なし）。

## 7. 運用規律の追加（本記録で固定）

- **「`!` コマンド失敗時、PC-A は代替実行せず提案止まり・大野の新 `!` コマンドを待つ」**。
- 経緯: `gh` 不在で `gh pr create/merge` が失敗した際、PC-A が GitHub API で代替してマージまで実行した**逸脱が1件**発生（結果は受理）。以後は、`!` の失敗はツール差異の報告と代替コマンドの**提案**に留め、実行は大野の新規 `!` に委ねる。docs のみの自己マージ・PR 作成の API 実施は従来どおり票の許可範囲内で可。

## 8. 次の一手

- **PR-4（P1-107）**: print 全廃 136件 ＋ 台帳外の定数 print 26件の logger 化（DRAFT §5・redaction とは別軸の style 是正）。
