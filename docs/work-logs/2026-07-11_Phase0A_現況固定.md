# 作業記録 2026-07-11: Phase 0A 現況固定（クローズ）

- Phase/Gate: **Phase 0A 完了・G0判定待ち**
- 記録日: 2026-07-11 ／ 実施: Claude Code（PC-A）／ タスク: P0A-001〜006
- 出典: 各タスクのCOMPLETION_REPORT実出力・R0-A（Codex）裁定・本repo実物。
  **記憶・推測による補完はしていない**（推測箇所はその旨明記）

## 1. 確定事項

| 項目 | 確定値 | 根拠 |
|---|---|---|
| **G0固定SHA** | `2822f898d1d67f7de0f282b8e61c5edbb0919ae2`（local main = origin/main） | P0A-002でfetch・P0A-004でff-only pull。7/7成果（PR#78〜96＋直接merge1件・50ファイル+9,637行）はorigin/mainに全量収載を確認 |
| **baseline** | **1,059 passed / FAIL 0 / skip 0 / xfail 0（＋210 subtests passed）** | 機械可読証拠: [`2026-07-11_phase0a_baseline.txt`](./2026-07-11_phase0a_baseline.txt)（collect実出力1,059件・全テストのPASSED行・pip freeze・Python 3.12.10） |
| baseline除外 | `test_triage_classification.py`（トリアージ回帰120ケース） | 実Claude API・実kintone到達のため（conftest.py:8-16）。実測は `railway run` で別途（開発中はTRIAGE_SCOPE=diff・全量はマージ直前＋週次） |
| 環境注記 | pymupdf 1.28.0 をこのPCに導入（P0A-005・明示許可） | requirements.txt:10記載どおり。導入前はtest_document_splitter.py 2件が `ModuleNotFoundError: fitz` でFAIL |
| **deny防御** | ユーザーレベル `~/.claude/settings.json` に22件投入済み（repo正本PR#57の鏡写し） | すり抜けテスト: P0A-004で**5経路**（①Bash直接 git push --dry-run ②Bash→powershell.exeラッパー ③PowerShellツール直接 ④curl ⑤Read .env相当）**全ブロック**・拒否メッセージ実出力取得済み。P0A-005で新規セッション再確認（経路①）もブロック |

## 2. RV確定状態表（2822f898時点）

番号は仮（P0A-001指示の列挙順。正本RV番号との突合はG0で実施）。

| # | 項目 | 状態 | 根拠（file:行） |
|---|---|---|---|
| RV-01 | /scan 認証 | **OPEN（無認証）** | main.py:937-938。token/署名/Dependsなし |
| RV-02 | /ocr/fixed-asset 認証 | **OPEN（無認証）** | main.py:729-730 |
| RV-03 | ingest群 token（koseki/registry/sortation/valuation/bank） | **部分FIXED / query方式OPEN** | hub/webhook_auth.py:14-20（env未設定=fail-closed・`hmac.compare_digest`・不一致404秘匿）。ただしtokenがquery/form渡しでURLログ残留リスク |
| RV-04 | LINE webhook署名（/webhook・/webhook/dispatch-bot） | **真正性FIXED / durability・idempotency OPEN** | 署名=HMAC-SHA256+compare_digest（main.py:248-253ほか）・dispatch側secret未設定deny-all＋ホワイトリスト。BackgroundTasks処理でevent ID dedupなし（`webhookEventId`/`isRedelivery`参照0件）＝再送二重処理リスク |
| RV-05 | Stripe webhook | **署名FIXED / idempotency OPEN** | `stripe.Webhook.construct_event`使用。`event["id"]` dedupなし→再送でkintone重複起票リスク |
| RV-06 | CloudSign webhook | **fail-open OPEN** | cloudsign_webhook.py:181-188: 真正性確認 `fetch_document` の**失敗を握りつぶして「受任」更新＋LINE通知に進む**・:190常時200。合言葉secret自体はcompare_digest |
| RV-07 | 業務通知token fallback | **OPEN（裁定衝突→NEEDS_HUMAN H04）** | hub/notify.py:31-43: DISPATCHBOT未設定時に顧客チャネルtokenへfallback（7/7裁定「警報欠落防止優先」の意図的実装）vs fail-closed方針の衝突。§4参照 |
| RV-08 | R4-2敗者レコード物理削除 | **実装済み・非可逆OPEN（B05）** | person_merge_exec.py:286 `delete_record`。安全装置: PERSON_MERGE_ENABLEDフラグ・人の二段確認のみで起動・監査JSON添付成功が削除の前提（:258-272）・復元手順（koseki-ocr/08）。物理削除ゆえ非可逆リスクは残置 |
| RV-09 | sortation process-memory | **OPEN（限定）** | sortation_ingest.py:61 `_seen_drive_file_ids`（dedupのみ・照会台帳はApp38永続） |
| RV-10 | /health依存反映 | **OPEN** | main.py:67〜: importチェックのみ・depsがNGでも`status:"ok"`固定 |
| RV-11 | scheduler | **OPEN（in-process asyncio）** | hub/scheduler.py:56-63・startup登録。再起動で消える（再登録設計） |
| RV-12 | requirements.txt固定 | **OPEN** | 全10行version無指定（==/>=ゼロ） |
| RV-13 | App38（仕分けログ）死活監視 | **OPEN（未登録）** | config.pyにAPP_SORTATION_LOGなし。台帳§2に未着手登録（13フィールド・検収済み実出力あり） |
| RV-14 | GAS主経路のpoison-pill | **OPEN** | legacy/gas/コード.js:14-42（既存3フォルダ→/scanループにtry/catchなし）。新設ブロック（戸籍/仕分け/登記等）は堅牢化済み。GAS正本はエディタ側＝実機状態はBLOCKED_NEEDS_HUMAN |

## 3. R0-A（Codex）裁定結果

- 指摘: **BLOCKER 5件（B01〜B05）・HIGH 7件・MEDIUM 4件・LOW 2件 → 全件ACCEPT**
- うち **B05・H04 は NEEDS_HUMAN**（§4）
- Phase 0B封じ込めの優先順位9項目が裁定済み（詳細は司令塔のR0-A裁定記録が正本。
  PC-Aは裁定文書の全文を保持していないため本記録には転記しない）

## 4. NEEDS_HUMAN 2件（大野の裁定待ち）

1. **H04（notify fallback）**: hub/notify.py:31-43 の顧客チャネルへのfallbackは
   2026-07-07裁定「警報の欠落防止を優先」による意図的実装（docstring・
   test_hub_notify.py:217-223でピン留め済み）だが、設計書のfail-closed方針と衝突。
   「fallback維持（警報優先）」か「fail-closed化（誤宛先防止優先）」かの裁定が必要。
   関連: main.py の顧客宛push 1箇所が business_token_env() を使用している点も同時に裁定対象。
2. **B05（PERSON_MERGE_ENABLED）**: R4-2物理削除経路の本番ON/OFF現状が未確認
   （Railway envの読み取りはPC-Aから禁止のため確認不能）。ONなら実機で削除経路が活性。
   現状確認と、Phase 0B完了までの明示OFF裁定が必要。

## 5. DISCOVERY（secret衛生・Phase 0B rotation候補）

- `docs/work-logs/2026-07-06_App33-34確定_deny強化_戸籍ライン開通.md` の46行目に
  **KOSEKI_INGEST_TOKEN の値の先頭数文字が平文記載**（値は本記録に転記しない）。
  → Phase 0B の secret rotation 候補として登録（該当tokenのローテーション＋当該行の削除）。

## 6. 未解決・次アクション

- **Phase 0B封じ込め**（本番到達性・credential・feature flag）は大野の朝の窓での承認待ち。
  この承認なしにPC-Aは実装・本番接続を行わない
- P1〜P16のうち **BLOCKED_NEEDS_HUMAN**（repo外のため大野の持ち込みが必要）:
  正本文書群（製品設計完全版v2.4・凍結ケース表・スプリント実行計画・7月/8月レポート）、
  テンプレ実物、契約、実機kintoneフォーム、production設定・env実値
- baseline環境差異: pymupdf導入済み（このPCのみ・repo変更なし）。
  triage回帰の実測値は railway run 実行時に別途確定

## 7. 翌タスク

朝の窓で **G0判定packet**（本記録＋baseline証拠＋P0A-001〜005報告）を提示 →
**Phase 0B着手判断**（封じ込め9項目の実行順・NEEDS_HUMAN 2件の裁定を含む）。
