# 作業記録 2026-08-09: koseki lane2 証跡③ retirement（credential 削除・実見固定）

- TASK_ID: KOSEKI-LANE2-RETIREMENT／実施: [人]（削除操作・実見）＋PC-A（docs のみ・コード変更なし）／記録日 2026-08-09
- 手順書: `docs/runbooks/2026-07_S4-S5_cutover-checklist.md`（D-7 証跡③）
- 正本: `DRAFT_RV04C_CALLER_MIGRATION.md` rev D5（凍結 commit `c32c45d`・main 外）§5.2 末尾
  「削除は当該 lane の段 4 安定確認後」——lane2 の D-5 安定は 7/20 投入以降の停止安定＋
  8/9 ゲート4クローズ（`2026-08-09_P2-koseki-lane2-D7-close.md`・能動404・legacy_blocked 3 件・
  legacy 成功 0 継続）で裏づけ済み。
- 先例形式: `2026-07-18_S5-cutover-close.md` §4(i)（sortation・削除実見 3 点セット・2026-07-27）と同型。
- **ステータス: クローズ（3 点充足・2026-08-09）**。(3) は §1.1 の追記（22:40 JST・[人]実見）で
  充足。lane2 retirement の完全クローズ裁定は司令塔（sortation 先例と同型）。
  - 履歴（同日 22:40 前の記録時点): 条件付きクローズ（(3) トリガー跨ぎ観測が [人] 確認待ち）。

## 1. 削除実見 3 点セット（S5-close §4(i) 同型・[人] 実施 2026-08-09）

| # | 項目 | 状態 | 実見内容 |
|---|---|---|---|
| (1) | Railway env `KOSEKI_INGEST_TOKEN` 削除 | **完了（2026-08-09・[人]）** | Railway Variables から削除・**削除後デプロイ green を実見** |
| (2) | live GAS 旧 token 定数削除 | **完了（2026-08-09・[人]）** | 「相続書類自動化」から **`KOSEKI_TOKEN` 定義行**と **legacyToken 使用 1 箇所**を削除（**legacyPayload は残置＝司令塔指定の形**）。保存後の**全文検索ヒット 0 を実見** |
| (3) | 削除後の signed lane 正常継続 | **完了（2026-08-09 22:40 JST・[人]・§1.1）** | 削除後の次トリガーサイクルがエラーなく完了（GAS「実行数」画面・状態=完了・失敗なし） |

- 実施時刻: [人] の削除操作は 2026-08-09（個別の時刻記録なし・sortation 先例 §4(i) の
  「時刻記録なし」と同型）。

### 1.1 (3) トリガー跨ぎ観測の結果（追記 2026-08-09）

- **結果（2026-08-09 22:40 JST・[人]実見）**: 削除後の**次トリガーサイクルの実行**が GAS
  「実行数」画面で**エラーなく完了**していることを実見（**状態=完了・失敗なし**）。
  ＝ live GAS からの定数削除（§1(2)）後もトリガー実行が正常＝削除による watcher 破壊なし。
- 観測形態の注記: 本サイクルの観測は「実行数」画面ベース（実行の完了・失敗有無）。
  対象ファイルの `[済]` リネーム／Railway 側 `reason=ok` の対応までは本欄の対象外
  （新規ファイル到来時の実送は従来どおり D-4 期の実績（7/20・200×3）と署名経路無変更の
  事実に依拠）。

## 2. checklist D-7 証跡③との対応

D-7 証跡③の定義（逐語）:
> 3. **credential 削除**: `*_INGEST_TOKEN` env 削除済み・GAS 旧 token 定数削除済み・承認キュー
>    旧 token が kintone URL から消えたこと（[人]実見）＋ 5-4 完了。

| 要素 | lane2 での充足 |
|---|---|
| `*_INGEST_TOKEN` env 削除済み | §1(1)＝**充足（2026-08-09）** |
| GAS 旧 token 定数削除済み | §1(2)＝**充足（2026-08-09）** |
| 承認キュー旧 token が kintone URL から消えたこと＋5-4 完了 | **lane1 の回で完了済み（7/18・D-6b rotation 4 工程＋5-4a〜c）**。lane 共通の承認キュー token は再実施不要 |

→ 証跡③は §1.1 の追記（2026-08-09 22:40 JST）をもって**充足**。lane2 retirement 全体は
①署名成功実送（7/20・200×3・`2026-07-20_P2-koseki-cutover.md` §6.1）＋
②能動404＋計数（8/9・`2026-08-09_P2-koseki-lane2-D7-close.md`）＋③（本書）で 3 点構成。

## 3. 凍結写しとの差分注記（本書での記録のみ・写しは触らない）

- `legacy/gas/コード.js`（旧・署名前世代の**凍結写し**）には `KOSEKI_TOKEN` の
  **定義（12 行目・プレースホルダ値）と使用（55 行目）が残存**する。
- 本日の live GAS 側削除により、**凍結写しと live の間に差分が生じた**（live には
  `KOSEKI_TOKEN` 定義・legacyToken 使用が存在しない。全文検索ヒット 0・[人]実見）。
- 写しは凍結扱いのため**更新しない**（sortation の先例と同じ扱い——`SORTATION_TOKEN` も
  live 削除済み・写し残存）。写しの位置づけ変更（凍結解除・注記追加等）は司令塔裁定事項。

## 4. 併施した衛生処理（同 commit・規律17・司令塔裁定）

- `docs/work-logs/2026-07-06_App33-34確定_deny強化_戸籍ライン開通.md` 46 行目に残存していた
  **KOSEKI_INGEST_TOKEN の値の先頭断片（6 文字）をマスク**へ置換
  （注記形式:「（マスク済み 2026-08-09・規律17に基づく衛生処理・司令塔裁定）」）。
- 対象は当該断片 1 箇所のみ・他の記述は不変。当該 token は本書 §1(1) で削除済みのため
  断片の実効性も消滅しているが、記録衛生として処理（8/9 ブロックB準備報告の付随観察 1 件に
  対する司令塔裁定に基づく）。git 履歴上に断片が残る点は git の性質上不可避（履歴書換は
  行わない・リスクは token 自体の失効により消滅）。

## 5. 枠消化の日次一行

- 2026-08-09: koseki lane2 証跡③ retirement work-log 固定（env 削除・GAS 定数削除の
  [人]実見 2 点＝完了・(3) トリガー跨ぎ観測は [人] 確認待ちの追記欄・7/06 work-log の
  値断片マスク併施・凍結写し差分の注記）。実施 = [人]（削除）＋PC-A（docs のみ）。
  モデル実測 = Fable 5。
- 2026-08-09（追記・22:40 JST）: (3) トリガー跨ぎ観測の [人]実見を §1.1 へ追記——
  削除後の次トリガーサイクルが「実行数」画面でエラーなく完了（状態=完了・失敗なし）。
  **ステータスをクローズ（3 点充足）へ更新**（完全クローズ裁定は司令塔）。
